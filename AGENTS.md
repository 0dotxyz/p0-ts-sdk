# p0-ts-sdk — agent instructions

Read [code_rules.md](code_rules.md) before writing or reviewing code; its rules are hard requirements.

After editing a file under `src`, run `pnpm exec eslint --fix <file>` and `pnpm exec prettier --write <file>`.

Before finishing: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm knip`.
