/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { KAMINO_LENDING_PROGRAM_ADDRESS } from "../programs";

export const CALCULATE_CTOKEN_EXCHANGE_RATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  32, 253, 220, 13, 19, 165, 131, 188,
]);

export function getCalculateCtokenExchangeRateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CALCULATE_CTOKEN_EXCHANGE_RATE_DISCRIMINATOR);
}

export type CalculateCtokenExchangeRateInstruction<
  TProgram extends string = typeof KAMINO_LENDING_PROGRAM_ADDRESS,
  TAccountReserve extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReserve extends string ? ReadonlyAccount<TAccountReserve> : TAccountReserve,
      ...TRemainingAccounts,
    ]
  >;

export type CalculateCtokenExchangeRateInstructionData = { discriminator: ReadonlyUint8Array };

export type CalculateCtokenExchangeRateInstructionDataArgs = {};

export function getCalculateCtokenExchangeRateInstructionDataEncoder(): FixedSizeEncoder<CalculateCtokenExchangeRateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CALCULATE_CTOKEN_EXCHANGE_RATE_DISCRIMINATOR })
  );
}

export function getCalculateCtokenExchangeRateInstructionDataDecoder(): FixedSizeDecoder<CalculateCtokenExchangeRateInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getCalculateCtokenExchangeRateInstructionDataCodec(): FixedSizeCodec<
  CalculateCtokenExchangeRateInstructionDataArgs,
  CalculateCtokenExchangeRateInstructionData
> {
  return combineCodec(
    getCalculateCtokenExchangeRateInstructionDataEncoder(),
    getCalculateCtokenExchangeRateInstructionDataDecoder()
  );
}

export type CalculateCtokenExchangeRateInput<
  TAccountReserve extends InstructionAccountInput = InstructionAccountInput,
> = {
  reserve: TAccountReserve;
};

export function getCalculateCtokenExchangeRateInstruction<
  TAccountReserve extends InstructionAccountInput,
  TProgramAddress extends Address = typeof KAMINO_LENDING_PROGRAM_ADDRESS,
>(
  input: CalculateCtokenExchangeRateInput<TAccountReserve>,
  config?: { programAddress?: TProgramAddress }
): CalculateCtokenExchangeRateInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountReserve, InstructionAccountInputAddress<TAccountReserve>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? KAMINO_LENDING_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    reserve: { value: input.reserve ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [getAccountMeta("reserve", accounts.reserve)],
    data: getCalculateCtokenExchangeRateInstructionDataEncoder().encode({}),
    programAddress,
  } as CalculateCtokenExchangeRateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountReserve, InstructionAccountInputAddress<TAccountReserve>>
  >);
}

export type ParsedCalculateCtokenExchangeRateInstruction<
  TProgram extends string = typeof KAMINO_LENDING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    reserve: TAccountMetas[0];
  };
  data: CalculateCtokenExchangeRateInstructionData;
};

export function parseCalculateCtokenExchangeRateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCalculateCtokenExchangeRateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 1,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { reserve: getNextAccount() },
    data: getCalculateCtokenExchangeRateInstructionDataDecoder().decode(instruction.data),
  };
}
