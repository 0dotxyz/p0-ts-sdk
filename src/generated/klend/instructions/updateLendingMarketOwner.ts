/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { KAMINO_LENDING_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_LENDING_MARKET_OWNER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  118, 224, 10, 62, 196, 230, 184, 89,
]);

export function getUpdateLendingMarketOwnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_LENDING_MARKET_OWNER_DISCRIMINATOR);
}

export type UpdateLendingMarketOwnerInstruction<
  TProgram extends string = typeof KAMINO_LENDING_PROGRAM_ADDRESS,
  TAccountLendingMarketOwnerCached extends string | AccountMeta<string> = string,
  TAccountLendingMarket extends string | AccountMeta<string> = string,
  TAccountInstructionSysvarAccount extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLendingMarketOwnerCached extends string
        ? ReadonlySignerAccount<TAccountLendingMarketOwnerCached> &
            AccountSignerMeta<TAccountLendingMarketOwnerCached>
        : TAccountLendingMarketOwnerCached,
      TAccountLendingMarket extends string
        ? WritableAccount<TAccountLendingMarket>
        : TAccountLendingMarket,
      TAccountInstructionSysvarAccount extends string
        ? ReadonlyAccount<TAccountInstructionSysvarAccount>
        : TAccountInstructionSysvarAccount,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateLendingMarketOwnerInstructionData = { discriminator: ReadonlyUint8Array };

export type UpdateLendingMarketOwnerInstructionDataArgs = {};

export function getUpdateLendingMarketOwnerInstructionDataEncoder(): FixedSizeEncoder<UpdateLendingMarketOwnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UPDATE_LENDING_MARKET_OWNER_DISCRIMINATOR })
  );
}

export function getUpdateLendingMarketOwnerInstructionDataDecoder(): FixedSizeDecoder<UpdateLendingMarketOwnerInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getUpdateLendingMarketOwnerInstructionDataCodec(): FixedSizeCodec<
  UpdateLendingMarketOwnerInstructionDataArgs,
  UpdateLendingMarketOwnerInstructionData
> {
  return combineCodec(
    getUpdateLendingMarketOwnerInstructionDataEncoder(),
    getUpdateLendingMarketOwnerInstructionDataDecoder()
  );
}

export type UpdateLendingMarketOwnerInput<
  TAccountLendingMarketOwnerCached extends InstructionSignerInput = InstructionSignerInput,
  TAccountLendingMarket extends InstructionAccountInput = InstructionAccountInput,
  TAccountInstructionSysvarAccount extends InstructionAccountInput = InstructionAccountInput,
> = {
  lendingMarketOwnerCached: TAccountLendingMarketOwnerCached;
  lendingMarket: TAccountLendingMarket;
  instructionSysvarAccount?: TAccountInstructionSysvarAccount;
};

export function getUpdateLendingMarketOwnerInstruction<
  TAccountLendingMarketOwnerCached extends InstructionSignerInput,
  TAccountLendingMarket extends InstructionAccountInput,
  TAccountInstructionSysvarAccount extends InstructionAccountInput,
  TProgramAddress extends Address = typeof KAMINO_LENDING_PROGRAM_ADDRESS,
>(
  input: UpdateLendingMarketOwnerInput<
    TAccountLendingMarketOwnerCached,
    TAccountLendingMarket,
    TAccountInstructionSysvarAccount
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateLendingMarketOwnerInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountLendingMarketOwnerCached,
    InstructionAccountInputAddress<TAccountLendingMarketOwnerCached>
  >,
  ResolvedInstructionAccountMeta<
    TAccountLendingMarket,
    InstructionAccountInputAddress<TAccountLendingMarket>
  >,
  ResolvedInstructionAccountMeta<
    TAccountInstructionSysvarAccount,
    InstructionAccountInputAddress<TAccountInstructionSysvarAccount>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? KAMINO_LENDING_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    lendingMarketOwnerCached: {
      value: input.lendingMarketOwnerCached ?? null,
      isSigner: true,
      isWritable: false,
    },
    lendingMarket: { value: input.lendingMarket ?? null, isSigner: false, isWritable: true },
    instructionSysvarAccount: {
      value: input.instructionSysvarAccount ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.instructionSysvarAccount.value) {
    accounts.instructionSysvarAccount.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("lendingMarketOwnerCached", accounts.lendingMarketOwnerCached),
      getAccountMeta("lendingMarket", accounts.lendingMarket),
      getAccountMeta("instructionSysvarAccount", accounts.instructionSysvarAccount),
    ],
    data: getUpdateLendingMarketOwnerInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateLendingMarketOwnerInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountLendingMarketOwnerCached,
      InstructionAccountInputAddress<TAccountLendingMarketOwnerCached>
    >,
    ResolvedInstructionAccountMeta<
      TAccountLendingMarket,
      InstructionAccountInputAddress<TAccountLendingMarket>
    >,
    ResolvedInstructionAccountMeta<
      TAccountInstructionSysvarAccount,
      InstructionAccountInputAddress<TAccountInstructionSysvarAccount>
    >
  >);
}

export type ParsedUpdateLendingMarketOwnerInstruction<
  TProgram extends string = typeof KAMINO_LENDING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    lendingMarketOwnerCached: TAccountMetas[0];
    lendingMarket: TAccountMetas[1];
    instructionSysvarAccount: TAccountMetas[2];
  };
  data: UpdateLendingMarketOwnerInstructionData;
};

export function parseUpdateLendingMarketOwnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateLendingMarketOwnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      lendingMarketOwnerCached: getNextAccount(),
      lendingMarket: getNextAccount(),
      instructionSysvarAccount: getNextAccount(),
    },
    data: getUpdateLendingMarketOwnerInstructionDataDecoder().decode(instruction.data),
  };
}
