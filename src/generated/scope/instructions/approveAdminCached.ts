/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { SCOPE_PROGRAM_ADDRESS } from "../programs";

export const APPROVE_ADMIN_CACHED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  101, 149, 97, 58, 48, 79, 16, 105,
]);

export function getApproveAdminCachedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(APPROVE_ADMIN_CACHED_DISCRIMINATOR);
}

export type ApproveAdminCachedInstruction<
  TProgram extends string = typeof SCOPE_PROGRAM_ADDRESS,
  TAccountAdminCached extends string | AccountMeta<string> = string,
  TAccountConfiguration extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdminCached extends string
        ? ReadonlySignerAccount<TAccountAdminCached> & AccountSignerMeta<TAccountAdminCached>
        : TAccountAdminCached,
      TAccountConfiguration extends string
        ? WritableAccount<TAccountConfiguration>
        : TAccountConfiguration,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveAdminCachedInstructionData = {
  discriminator: ReadonlyUint8Array;
  feedName: string;
};

export type ApproveAdminCachedInstructionDataArgs = { feedName: string };

export function getApproveAdminCachedInstructionDataEncoder(): Encoder<ApproveAdminCachedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["feedName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: APPROVE_ADMIN_CACHED_DISCRIMINATOR })
  );
}

export function getApproveAdminCachedInstructionDataDecoder(): Decoder<ApproveAdminCachedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["feedName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getApproveAdminCachedInstructionDataCodec(): Codec<
  ApproveAdminCachedInstructionDataArgs,
  ApproveAdminCachedInstructionData
> {
  return combineCodec(
    getApproveAdminCachedInstructionDataEncoder(),
    getApproveAdminCachedInstructionDataDecoder()
  );
}

export type ApproveAdminCachedInput<
  TAccountAdminCached extends InstructionSignerInput = InstructionSignerInput,
  TAccountConfiguration extends InstructionAccountInput = InstructionAccountInput,
> = {
  adminCached: TAccountAdminCached;
  configuration: TAccountConfiguration;
  feedName: ApproveAdminCachedInstructionDataArgs["feedName"];
};

export function getApproveAdminCachedInstruction<
  TAccountAdminCached extends InstructionSignerInput,
  TAccountConfiguration extends InstructionAccountInput,
  TProgramAddress extends Address = typeof SCOPE_PROGRAM_ADDRESS,
>(
  input: ApproveAdminCachedInput<TAccountAdminCached, TAccountConfiguration>,
  config?: { programAddress?: TProgramAddress }
): ApproveAdminCachedInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountAdminCached,
    InstructionAccountInputAddress<TAccountAdminCached>
  >,
  ResolvedInstructionAccountMeta<
    TAccountConfiguration,
    InstructionAccountInputAddress<TAccountConfiguration>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SCOPE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    adminCached: { value: input.adminCached ?? null, isSigner: true, isWritable: false },
    configuration: { value: input.configuration ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("adminCached", accounts.adminCached),
      getAccountMeta("configuration", accounts.configuration),
    ],
    data: getApproveAdminCachedInstructionDataEncoder().encode(
      args as ApproveAdminCachedInstructionDataArgs
    ),
    programAddress,
  } as ApproveAdminCachedInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountAdminCached,
      InstructionAccountInputAddress<TAccountAdminCached>
    >,
    ResolvedInstructionAccountMeta<
      TAccountConfiguration,
      InstructionAccountInputAddress<TAccountConfiguration>
    >
  >);
}

export type ParsedApproveAdminCachedInstruction<
  TProgram extends string = typeof SCOPE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    adminCached: TAccountMetas[0];
    configuration: TAccountMetas[1];
  };
  data: ApproveAdminCachedInstructionData;
};

export function parseApproveAdminCachedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApproveAdminCachedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { adminCached: getNextAccount(), configuration: getNextAccount() },
    data: getApproveAdminCachedInstructionDataDecoder().decode(instruction.data),
  };
}
