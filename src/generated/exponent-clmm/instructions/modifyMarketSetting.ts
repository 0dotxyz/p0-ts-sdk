/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { EXPONENT_CLMM_PROGRAM_ADDRESS } from "../programs";
import {
  getMarketAdminActionDecoder,
  getMarketAdminActionEncoder,
  type MarketAdminAction,
  type MarketAdminActionArgs,
} from "../types";

export const MODIFY_MARKET_SETTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([10]);

export function getModifyMarketSettingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 1).encode(MODIFY_MARKET_SETTING_DISCRIMINATOR);
}

export type ModifyMarketSettingInstruction<
  TProgram extends string = typeof EXPONENT_CLMM_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountAdminState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountAdminState extends string ? ReadonlyAccount<TAccountAdminState> : TAccountAdminState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ModifyMarketSettingInstructionData = {
  discriminator: ReadonlyUint8Array;
  action: MarketAdminAction;
};

export type ModifyMarketSettingInstructionDataArgs = { action: MarketAdminActionArgs };

export function getModifyMarketSettingInstructionDataEncoder(): Encoder<ModifyMarketSettingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 1)],
      ["action", getMarketAdminActionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: MODIFY_MARKET_SETTING_DISCRIMINATOR })
  );
}

export function getModifyMarketSettingInstructionDataDecoder(): Decoder<ModifyMarketSettingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
    ["action", getMarketAdminActionDecoder()],
  ]);
}

export function getModifyMarketSettingInstructionDataCodec(): Codec<
  ModifyMarketSettingInstructionDataArgs,
  ModifyMarketSettingInstructionData
> {
  return combineCodec(
    getModifyMarketSettingInstructionDataEncoder(),
    getModifyMarketSettingInstructionDataDecoder()
  );
}

export type ModifyMarketSettingInput<
  TAccountMarket extends InstructionAccountInput = InstructionAccountInput,
  TAccountSigner extends InstructionSignerInput = InstructionSignerInput,
  TAccountAdminState extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  market: TAccountMarket;
  signer: TAccountSigner;
  adminState: TAccountAdminState;
  systemProgram?: TAccountSystemProgram;
  action: ModifyMarketSettingInstructionDataArgs["action"];
};

export function getModifyMarketSettingInstruction<
  TAccountMarket extends InstructionAccountInput,
  TAccountSigner extends InstructionSignerInput,
  TAccountAdminState extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof EXPONENT_CLMM_PROGRAM_ADDRESS,
>(
  input: ModifyMarketSettingInput<
    TAccountMarket,
    TAccountSigner,
    TAccountAdminState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ModifyMarketSettingInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>,
  ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
  ResolvedInstructionAccountMeta<
    TAccountAdminState,
    InstructionAccountInputAddress<TAccountAdminState>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? EXPONENT_CLMM_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isSigner: false, isWritable: true },
    signer: { value: input.signer ?? null, isSigner: true, isWritable: true },
    adminState: { value: input.adminState ?? null, isSigner: false, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("market", accounts.market),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("adminState", accounts.adminState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getModifyMarketSettingInstructionDataEncoder().encode(
      args as ModifyMarketSettingInstructionDataArgs
    ),
    programAddress,
  } as ModifyMarketSettingInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountMarket, InstructionAccountInputAddress<TAccountMarket>>,
    ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
    ResolvedInstructionAccountMeta<
      TAccountAdminState,
      InstructionAccountInputAddress<TAccountAdminState>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedModifyMarketSettingInstruction<
  TProgram extends string = typeof EXPONENT_CLMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    market: TAccountMetas[0];
    signer: TAccountMetas[1];
    adminState: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ModifyMarketSettingInstructionData;
};

export function parseModifyMarketSettingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedModifyMarketSettingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      signer: getNextAccount(),
      adminState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getModifyMarketSettingInstructionDataDecoder().decode(instruction.data),
  };
}
