/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getCrossingSplitDecoder,
  getCrossingSplitEncoder,
  getPersonalYieldTrackersDecoder,
  getPersonalYieldTrackersEncoder,
  getPrincipalShareTrackersDecoder,
  getPrincipalShareTrackersEncoder,
  type CrossingSplit,
  type CrossingSplitArgs,
  type PersonalYieldTrackers,
  type PersonalYieldTrackersArgs,
  type PrincipalShareTrackers,
  type PrincipalShareTrackersArgs,
} from "../types";

export const LP_POSITION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  105, 241, 37, 200, 224, 2, 252, 90,
]);

export function getLpPositionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LP_POSITION_DISCRIMINATOR);
}

export type LpPosition = {
  discriminator: ReadonlyUint8Array;
  /** Link to address that owns this position */
  owner: Address;
  /** Link to market that manages the LP */
  market: Address;
  /** last fee calc position snapshot Q64.64 */
  feeInsideLastPt: bigint;
  /** last fee calc position snapshot Q64.64 */
  feeInsideLastSy: bigint;
  /** Track the LP balance of the user here */
  lpBalance: bigint;
  tokensOwedSy: bigint;
  tokensOwedPt: bigint;
  /** Lower tick of the position */
  lowerTickIdx: number;
  /** Upper tick of the position */
  upperTickIdx: number;
  farms: PersonalYieldTrackers;
  /** Share trackers for each tick in range */
  shareTrackers: PrincipalShareTrackers;
  /**
   * Crossing split for positions that span the active tick.
   * When active (is_active() returns true), the position is logically split
   * into 3 segments with the crossing segment having reduced liquidity (L_actual).
   * When inactive, this field is ignored and standard fee calculation is used.
   */
  crossingSplit: CrossingSplit;
};

export type LpPositionArgs = {
  /** Link to address that owns this position */
  owner: Address;
  /** Link to market that manages the LP */
  market: Address;
  /** last fee calc position snapshot Q64.64 */
  feeInsideLastPt: number | bigint;
  /** last fee calc position snapshot Q64.64 */
  feeInsideLastSy: number | bigint;
  /** Track the LP balance of the user here */
  lpBalance: number | bigint;
  tokensOwedSy: number | bigint;
  tokensOwedPt: number | bigint;
  /** Lower tick of the position */
  lowerTickIdx: number;
  /** Upper tick of the position */
  upperTickIdx: number;
  farms: PersonalYieldTrackersArgs;
  /** Share trackers for each tick in range */
  shareTrackers: PrincipalShareTrackersArgs;
  /**
   * Crossing split for positions that span the active tick.
   * When active (is_active() returns true), the position is logically split
   * into 3 segments with the crossing segment having reduced liquidity (L_actual).
   * When inactive, this field is ignored and standard fee calculation is used.
   */
  crossingSplit: CrossingSplitArgs;
};

/** Gets the encoder for {@link LpPositionArgs} account data. */
export function getLpPositionEncoder(): Encoder<LpPositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["market", getAddressEncoder()],
      ["feeInsideLastPt", getU128Encoder()],
      ["feeInsideLastSy", getU128Encoder()],
      ["lpBalance", getU64Encoder()],
      ["tokensOwedSy", getU64Encoder()],
      ["tokensOwedPt", getU64Encoder()],
      ["lowerTickIdx", getU32Encoder()],
      ["upperTickIdx", getU32Encoder()],
      ["farms", getPersonalYieldTrackersEncoder()],
      ["shareTrackers", getPrincipalShareTrackersEncoder()],
      ["crossingSplit", getCrossingSplitEncoder()],
    ]),
    (value) => ({ ...value, discriminator: LP_POSITION_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link LpPosition} account data. */
export function getLpPositionDecoder(): Decoder<LpPosition> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["market", getAddressDecoder()],
    ["feeInsideLastPt", getU128Decoder()],
    ["feeInsideLastSy", getU128Decoder()],
    ["lpBalance", getU64Decoder()],
    ["tokensOwedSy", getU64Decoder()],
    ["tokensOwedPt", getU64Decoder()],
    ["lowerTickIdx", getU32Decoder()],
    ["upperTickIdx", getU32Decoder()],
    ["farms", getPersonalYieldTrackersDecoder()],
    ["shareTrackers", getPrincipalShareTrackersDecoder()],
    ["crossingSplit", getCrossingSplitDecoder()],
  ]);
}

/** Gets the codec for {@link LpPosition} account data. */
export function getLpPositionCodec(): Codec<LpPositionArgs, LpPosition> {
  return combineCodec(getLpPositionEncoder(), getLpPositionDecoder());
}

export function decodeLpPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<LpPosition, TAddress>;
export function decodeLpPosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<LpPosition, TAddress>;
export function decodeLpPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<LpPosition, TAddress> | MaybeAccount<LpPosition, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLpPositionDecoder());
}

export async function fetchLpPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<LpPosition, TAddress>> {
  const maybeAccount = await fetchMaybeLpPosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLpPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<LpPosition, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLpPosition(maybeAccount);
}

export async function fetchAllLpPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<LpPosition>[]> {
  const maybeAccounts = await fetchAllMaybeLpPosition(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLpPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<LpPosition>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLpPosition(maybeAccount));
}
