/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getFeeStateCacheDecoder,
  getFeeStateCacheEncoder,
  getGroupRateLimiterDecoder,
  getGroupRateLimiterEncoder,
  getPanicStateCacheDecoder,
  getPanicStateCacheEncoder,
  getWithdrawWindowCacheDecoder,
  getWithdrawWindowCacheEncoder,
  type FeeStateCache,
  type FeeStateCacheArgs,
  type GroupRateLimiter,
  type GroupRateLimiterArgs,
  type PanicStateCache,
  type PanicStateCacheArgs,
  type WithdrawWindowCache,
  type WithdrawWindowCacheArgs,
} from "../types";

export const MARGINFI_GROUP_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  182, 23, 173, 240, 151, 206, 182, 67,
]);

export function getMarginfiGroupDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MARGINFI_GROUP_DISCRIMINATOR);
}

export type MarginfiGroup = {
  discriminator: ReadonlyUint8Array;
  /** Broadly able to modify anything, and can set/remove other admins at will. */
  admin: Address;
  /**
   * Bitmask for group settings flags.
   * * Bit 0 (1): `PROGRAM_FEES_ENABLED` — If set, program-level fees are enabled.
   * * Bits 1-63: Reserved for future use.
   */
  groupFlags: bigint;
  /** Caches information from the global `FeeState` so the FeeState can be omitted on certain ixes */
  feeStateCache: FeeStateCache;
  /**
   * For groups initialized in versions 0.1.2 or greater, this is an authoritative count
   * of the number of banks under this group. For groups initialized prior to 0.1.2,
   * a non-authoritative count of the number of banks initiated after 0.1.2 went live.
   */
  banks: number;
  pad0: ReadonlyUint8Array;
  /**
   * This admin can configure collateral ratios above (but not below) the collateral ratio of
   * certain banks, e.g. allow SOL to count as 90% collateral when borrowing an LST instead of
   * the default rate.
   */
  emodeAdmin: Address;
  /**
   * Can modify the fields in `config.interest_rate_config` but nothing else, for every bank
   * under this group
   */
  delegateCurveAdmin: Address;
  /**
   * Can modify the `deposit_limit`, `borrow_limit`, `total_asset_value_init_limit` but nothing
   * else, for every bank under this group
   */
  delegateLimitAdmin: Address;
  /**
   * DEPRECATED: currently has no on-chain authority.
   * Preserved in account layout for backward compatibility and historical metadata only.
   */
  delegateEmissionsAdmin: Address;
  /**
   * When program keeper temporarily puts the program into panic mode, information about the
   * duration of the lockup will be available here.
   */
  panicStateCache: PanicStateCache;
  /**
   * Keeps track of the liquidity withdrawn from the group over the day as a result of
   * deleverages. Used as a protection mechanism against too big (and unwanted) withdrawals (e.g.
   * when the risk admin is compromised).
   */
  deleverageWithdrawWindowCache: WithdrawWindowCache;
  /** Can run bankruptcy and forced deleverage ixes to e.g. sunset risky/illiquid assets */
  riskAdmin: Address;
  /** Can modify a Bank's metadata, and nothing else. */
  metadataAdmin: Address;
  /**
   * Maximum leverage allowed for emode positions (initial margin), stored as u32 basis.
   * Use `u32_to_basis` to convert to I80F48. Range: 1-100.
   */
  emodeMaxInitLeverage: number;
  /**
   * Maximum leverage allowed for emode positions (maintenance margin), stored as u32 basis.
   * Must be > emode_max_init_leverage. Range: 1-100.
   */
  emodeMaxMaintLeverage: number;
  /**
   * Encoded same-asset automatic emode leverage for initial margin.
   * Decode with `u32_to_basis`. Same-asset treatment is disabled when the decoded leverage is
   * less than or equal to 1 and also requires each participating bank to opt in.
   */
  sameAssetEmodeInitLeverage: number;
  /**
   * Encoded same-asset automatic emode leverage for maintenance margin.
   * Decode with `u32_to_basis`. Ordering is validated in decoded space.
   */
  sameAssetEmodeMaintLeverage: number;
  /**
   * Rate limiter for controlling aggregate withdraw/borrow outflow across all banks.
   * Tracks net outflow in USD.
   */
  rateLimiter: GroupRateLimiter;
  /** Last slot covered by an admin group rate limiter aggregation update. */
  rateLimiterLastAdminUpdateSlot: bigint;
  /**
   * Monotonic sequence number for admin group rate limiter updates.
   * This is used to enforce strict ordering and prevent duplicate/replayed batches
   * when slot ranges overlap or multiple updates happen in the same slot.
   */
  rateLimiterLastAdminUpdateSeq: bigint;
  /** Last slot covered by an admin deleverage withdraw-limit aggregation update. */
  deleverageWithdrawLastAdminUpdateSlot: bigint;
  /** Monotonic sequence number for admin deleverage withdraw-limit updates. */
  deleverageWithdrawLastAdminUpdateSeq: bigint;
  /**
   * Can modify flow-control status for the group, i.e. update the withdraw caches with flow
   * information from banks. Typically this is a hot wallet that lives in e.g. some cron job. If
   * compromised, flow control can be effectively disabled until the admin is restored, which
   * does not itself compromise any funds, and is merely annoying.
   */
  delegateFlowAdmin: Address;
  padding0: Array<Array<bigint>>;
  padding1: Array<Array<bigint>>;
  padding2: Array<Array<bigint>>;
};

export type MarginfiGroupArgs = {
  /** Broadly able to modify anything, and can set/remove other admins at will. */
  admin: Address;
  /**
   * Bitmask for group settings flags.
   * * Bit 0 (1): `PROGRAM_FEES_ENABLED` — If set, program-level fees are enabled.
   * * Bits 1-63: Reserved for future use.
   */
  groupFlags: number | bigint;
  /** Caches information from the global `FeeState` so the FeeState can be omitted on certain ixes */
  feeStateCache: FeeStateCacheArgs;
  /**
   * For groups initialized in versions 0.1.2 or greater, this is an authoritative count
   * of the number of banks under this group. For groups initialized prior to 0.1.2,
   * a non-authoritative count of the number of banks initiated after 0.1.2 went live.
   */
  banks: number;
  pad0: ReadonlyUint8Array;
  /**
   * This admin can configure collateral ratios above (but not below) the collateral ratio of
   * certain banks, e.g. allow SOL to count as 90% collateral when borrowing an LST instead of
   * the default rate.
   */
  emodeAdmin: Address;
  /**
   * Can modify the fields in `config.interest_rate_config` but nothing else, for every bank
   * under this group
   */
  delegateCurveAdmin: Address;
  /**
   * Can modify the `deposit_limit`, `borrow_limit`, `total_asset_value_init_limit` but nothing
   * else, for every bank under this group
   */
  delegateLimitAdmin: Address;
  /**
   * DEPRECATED: currently has no on-chain authority.
   * Preserved in account layout for backward compatibility and historical metadata only.
   */
  delegateEmissionsAdmin: Address;
  /**
   * When program keeper temporarily puts the program into panic mode, information about the
   * duration of the lockup will be available here.
   */
  panicStateCache: PanicStateCacheArgs;
  /**
   * Keeps track of the liquidity withdrawn from the group over the day as a result of
   * deleverages. Used as a protection mechanism against too big (and unwanted) withdrawals (e.g.
   * when the risk admin is compromised).
   */
  deleverageWithdrawWindowCache: WithdrawWindowCacheArgs;
  /** Can run bankruptcy and forced deleverage ixes to e.g. sunset risky/illiquid assets */
  riskAdmin: Address;
  /** Can modify a Bank's metadata, and nothing else. */
  metadataAdmin: Address;
  /**
   * Maximum leverage allowed for emode positions (initial margin), stored as u32 basis.
   * Use `u32_to_basis` to convert to I80F48. Range: 1-100.
   */
  emodeMaxInitLeverage: number;
  /**
   * Maximum leverage allowed for emode positions (maintenance margin), stored as u32 basis.
   * Must be > emode_max_init_leverage. Range: 1-100.
   */
  emodeMaxMaintLeverage: number;
  /**
   * Encoded same-asset automatic emode leverage for initial margin.
   * Decode with `u32_to_basis`. Same-asset treatment is disabled when the decoded leverage is
   * less than or equal to 1 and also requires each participating bank to opt in.
   */
  sameAssetEmodeInitLeverage: number;
  /**
   * Encoded same-asset automatic emode leverage for maintenance margin.
   * Decode with `u32_to_basis`. Ordering is validated in decoded space.
   */
  sameAssetEmodeMaintLeverage: number;
  /**
   * Rate limiter for controlling aggregate withdraw/borrow outflow across all banks.
   * Tracks net outflow in USD.
   */
  rateLimiter: GroupRateLimiterArgs;
  /** Last slot covered by an admin group rate limiter aggregation update. */
  rateLimiterLastAdminUpdateSlot: number | bigint;
  /**
   * Monotonic sequence number for admin group rate limiter updates.
   * This is used to enforce strict ordering and prevent duplicate/replayed batches
   * when slot ranges overlap or multiple updates happen in the same slot.
   */
  rateLimiterLastAdminUpdateSeq: number | bigint;
  /** Last slot covered by an admin deleverage withdraw-limit aggregation update. */
  deleverageWithdrawLastAdminUpdateSlot: number | bigint;
  /** Monotonic sequence number for admin deleverage withdraw-limit updates. */
  deleverageWithdrawLastAdminUpdateSeq: number | bigint;
  /**
   * Can modify flow-control status for the group, i.e. update the withdraw caches with flow
   * information from banks. Typically this is a hot wallet that lives in e.g. some cron job. If
   * compromised, flow control can be effectively disabled until the admin is restored, which
   * does not itself compromise any funds, and is merely annoying.
   */
  delegateFlowAdmin: Address;
  padding0: Array<Array<number | bigint>>;
  padding1: Array<Array<number | bigint>>;
  padding2: Array<Array<number | bigint>>;
};

/** Gets the encoder for {@link MarginfiGroupArgs} account data. */
export function getMarginfiGroupEncoder(): FixedSizeEncoder<MarginfiGroupArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["groupFlags", getU64Encoder()],
      ["feeStateCache", getFeeStateCacheEncoder()],
      ["banks", getU16Encoder()],
      ["pad0", fixEncoderSize(getBytesEncoder(), 6)],
      ["emodeAdmin", getAddressEncoder()],
      ["delegateCurveAdmin", getAddressEncoder()],
      ["delegateLimitAdmin", getAddressEncoder()],
      ["delegateEmissionsAdmin", getAddressEncoder()],
      ["panicStateCache", getPanicStateCacheEncoder()],
      ["deleverageWithdrawWindowCache", getWithdrawWindowCacheEncoder()],
      ["riskAdmin", getAddressEncoder()],
      ["metadataAdmin", getAddressEncoder()],
      ["emodeMaxInitLeverage", getU32Encoder()],
      ["emodeMaxMaintLeverage", getU32Encoder()],
      ["sameAssetEmodeInitLeverage", getU32Encoder()],
      ["sameAssetEmodeMaintLeverage", getU32Encoder()],
      ["rateLimiter", getGroupRateLimiterEncoder()],
      ["rateLimiterLastAdminUpdateSlot", getU64Encoder()],
      ["rateLimiterLastAdminUpdateSeq", getU64Encoder()],
      ["deleverageWithdrawLastAdminUpdateSlot", getU64Encoder()],
      ["deleverageWithdrawLastAdminUpdateSeq", getU64Encoder()],
      ["delegateFlowAdmin", getAddressEncoder()],
      ["padding0", getArrayEncoder(getArrayEncoder(getU64Encoder(), { size: 2 }), { size: 2 })],
      ["padding1", getArrayEncoder(getArrayEncoder(getU64Encoder(), { size: 2 }), { size: 32 })],
      ["padding2", getArrayEncoder(getArrayEncoder(getU64Encoder(), { size: 32 }), { size: 32 })],
    ]),
    (value) => ({ ...value, discriminator: MARGINFI_GROUP_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link MarginfiGroup} account data. */
export function getMarginfiGroupDecoder(): FixedSizeDecoder<MarginfiGroup> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["groupFlags", getU64Decoder()],
    ["feeStateCache", getFeeStateCacheDecoder()],
    ["banks", getU16Decoder()],
    ["pad0", fixDecoderSize(getBytesDecoder(), 6)],
    ["emodeAdmin", getAddressDecoder()],
    ["delegateCurveAdmin", getAddressDecoder()],
    ["delegateLimitAdmin", getAddressDecoder()],
    ["delegateEmissionsAdmin", getAddressDecoder()],
    ["panicStateCache", getPanicStateCacheDecoder()],
    ["deleverageWithdrawWindowCache", getWithdrawWindowCacheDecoder()],
    ["riskAdmin", getAddressDecoder()],
    ["metadataAdmin", getAddressDecoder()],
    ["emodeMaxInitLeverage", getU32Decoder()],
    ["emodeMaxMaintLeverage", getU32Decoder()],
    ["sameAssetEmodeInitLeverage", getU32Decoder()],
    ["sameAssetEmodeMaintLeverage", getU32Decoder()],
    ["rateLimiter", getGroupRateLimiterDecoder()],
    ["rateLimiterLastAdminUpdateSlot", getU64Decoder()],
    ["rateLimiterLastAdminUpdateSeq", getU64Decoder()],
    ["deleverageWithdrawLastAdminUpdateSlot", getU64Decoder()],
    ["deleverageWithdrawLastAdminUpdateSeq", getU64Decoder()],
    ["delegateFlowAdmin", getAddressDecoder()],
    ["padding0", getArrayDecoder(getArrayDecoder(getU64Decoder(), { size: 2 }), { size: 2 })],
    ["padding1", getArrayDecoder(getArrayDecoder(getU64Decoder(), { size: 2 }), { size: 32 })],
    ["padding2", getArrayDecoder(getArrayDecoder(getU64Decoder(), { size: 32 }), { size: 32 })],
  ]);
}

/** Gets the codec for {@link MarginfiGroup} account data. */
export function getMarginfiGroupCodec(): FixedSizeCodec<MarginfiGroupArgs, MarginfiGroup> {
  return combineCodec(getMarginfiGroupEncoder(), getMarginfiGroupDecoder());
}

export function decodeMarginfiGroup<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<MarginfiGroup, TAddress>;
export function decodeMarginfiGroup<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<MarginfiGroup, TAddress>;
export function decodeMarginfiGroup<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<MarginfiGroup, TAddress> | MaybeAccount<MarginfiGroup, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getMarginfiGroupDecoder());
}

export async function fetchMarginfiGroup<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<MarginfiGroup, TAddress>> {
  const maybeAccount = await fetchMaybeMarginfiGroup(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMarginfiGroup<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<MarginfiGroup, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMarginfiGroup(maybeAccount);
}

export async function fetchAllMarginfiGroup(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<MarginfiGroup>[]> {
  const maybeAccounts = await fetchAllMaybeMarginfiGroup(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMarginfiGroup(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<MarginfiGroup>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMarginfiGroup(maybeAccount));
}

export function getMarginfiGroupSize(): number {
  return 9256;
}
