/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPanicStateDecoder,
  getPanicStateEncoder,
  getWrappedI80F48Decoder,
  getWrappedI80F48Encoder,
  type PanicState,
  type PanicStateArgs,
  type WrappedI80F48,
  type WrappedI80F48Args,
} from "../types";

export const FEE_STATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  63, 224, 16, 85, 193, 36, 235, 220,
]);

export function getFeeStateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FEE_STATE_DISCRIMINATOR);
}

export type FeeState = {
  discriminator: ReadonlyUint8Array;
  /** The fee state's own key. A PDA derived from just `b"feestate"` */
  key: Address;
  /** Can modify fees, pause the protocol, etc */
  globalFeeAdmin: Address;
  /**
   * The base wallet for all protocol fees. All SOL fees go to this wallet. All non-SOL fees go
   * to the cannonical ATA of this wallet for that asset.
   */
  globalFeeWallet: Address;
  /**
   * Flat fee in lamports paid to the global fee wallet when initiating an account transfer
   * (anti-spam; 5,000,000 lamports ~= $0.50). A stored 0 means "use the default"
   * (`DEFAULT_ACCOUNT_TRANSFER_FEE_LAMPORTS`), which preserves the legacy fee for FeeStates
   * created before this field existed.
   */
  accountTransferFee: number;
  placeholder0: ReadonlyUint8Array;
  /**
   * Flat fee assessed when a new bank is initialized, in lamports.
   * * In SOL, in native decimals.
   */
  bankInitFlatSolFee: number;
  bumpSeed: number;
  padding0: ReadonlyUint8Array;
  /**
   * Liquidators can claim at this premium, when liquidating an asset in receivership
   * liquidation, e.g. (1 + this) * amount repaid >= asset seized
   * * A percentage
   */
  liquidationMaxFee: WrappedI80F48;
  /**
   * Fee collected by the program owner from all groups
   * * A percentage
   */
  programFeeFixed: WrappedI80F48;
  /**
   * Fee collected by the program owner from all groups
   * * A percentage
   */
  programFeeRate: WrappedI80F48;
  /**
   * When the global admin pauses the protocol in the event of an emergency, information about
   * the pause duration will be stored here and propagated to groups.
   */
  panicState: PanicState;
  placeholder1: bigint;
  /**
   * Flat fee assessed for insurance/program use when a liquidation is executed
   * * In SOL, in native decimals.
   */
  liquidationFlatSolFee: number;
  /**
   * Flat fee assessed for preventing spam use when creating an order
   * * In SOL, in native decimals.
   */
  orderInitFlatSolFee: number;
  /**
   * Take-profit Orders can be executed at this premium, which Keepers are allowed to keep (no
   * pun intended) e.g. (1 + this) * amount repaid >= asset seized
   * * A percentage
   */
  orderExecutionMaxFee: WrappedI80F48;
  /** Can pause (not unpause) the protocol, but cannot modify any fee configuration. */
  pauseDelegateAdmin: Address;
  /**
   * Reserved for future use (e.g. the variable-borrow premium settings). Accounts created
   * before the struct grew to this size are v1-sized (`8 + V1_LEN` bytes) and must be
   * grown via `resize_global_fee_state` before this program version can load them; the new
   * bytes are zero-filled.
   */
  reserved0: Array<bigint>;
};

export type FeeStateArgs = {
  /** The fee state's own key. A PDA derived from just `b"feestate"` */
  key: Address;
  /** Can modify fees, pause the protocol, etc */
  globalFeeAdmin: Address;
  /**
   * The base wallet for all protocol fees. All SOL fees go to this wallet. All non-SOL fees go
   * to the cannonical ATA of this wallet for that asset.
   */
  globalFeeWallet: Address;
  /**
   * Flat fee in lamports paid to the global fee wallet when initiating an account transfer
   * (anti-spam; 5,000,000 lamports ~= $0.50). A stored 0 means "use the default"
   * (`DEFAULT_ACCOUNT_TRANSFER_FEE_LAMPORTS`), which preserves the legacy fee for FeeStates
   * created before this field existed.
   */
  accountTransferFee: number;
  placeholder0: ReadonlyUint8Array;
  /**
   * Flat fee assessed when a new bank is initialized, in lamports.
   * * In SOL, in native decimals.
   */
  bankInitFlatSolFee: number;
  bumpSeed: number;
  padding0: ReadonlyUint8Array;
  /**
   * Liquidators can claim at this premium, when liquidating an asset in receivership
   * liquidation, e.g. (1 + this) * amount repaid >= asset seized
   * * A percentage
   */
  liquidationMaxFee: WrappedI80F48Args;
  /**
   * Fee collected by the program owner from all groups
   * * A percentage
   */
  programFeeFixed: WrappedI80F48Args;
  /**
   * Fee collected by the program owner from all groups
   * * A percentage
   */
  programFeeRate: WrappedI80F48Args;
  /**
   * When the global admin pauses the protocol in the event of an emergency, information about
   * the pause duration will be stored here and propagated to groups.
   */
  panicState: PanicStateArgs;
  placeholder1: number | bigint;
  /**
   * Flat fee assessed for insurance/program use when a liquidation is executed
   * * In SOL, in native decimals.
   */
  liquidationFlatSolFee: number;
  /**
   * Flat fee assessed for preventing spam use when creating an order
   * * In SOL, in native decimals.
   */
  orderInitFlatSolFee: number;
  /**
   * Take-profit Orders can be executed at this premium, which Keepers are allowed to keep (no
   * pun intended) e.g. (1 + this) * amount repaid >= asset seized
   * * A percentage
   */
  orderExecutionMaxFee: WrappedI80F48Args;
  /** Can pause (not unpause) the protocol, but cannot modify any fee configuration. */
  pauseDelegateAdmin: Address;
  /**
   * Reserved for future use (e.g. the variable-borrow premium settings). Accounts created
   * before the struct grew to this size are v1-sized (`8 + V1_LEN` bytes) and must be
   * grown via `resize_global_fee_state` before this program version can load them; the new
   * bytes are zero-filled.
   */
  reserved0: Array<number | bigint>;
};

/** Gets the encoder for {@link FeeStateArgs} account data. */
export function getFeeStateEncoder(): FixedSizeEncoder<FeeStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["key", getAddressEncoder()],
      ["globalFeeAdmin", getAddressEncoder()],
      ["globalFeeWallet", getAddressEncoder()],
      ["accountTransferFee", getU32Encoder()],
      ["placeholder0", fixEncoderSize(getBytesEncoder(), 4)],
      ["bankInitFlatSolFee", getU32Encoder()],
      ["bumpSeed", getU8Encoder()],
      ["padding0", fixEncoderSize(getBytesEncoder(), 3)],
      ["liquidationMaxFee", getWrappedI80F48Encoder()],
      ["programFeeFixed", getWrappedI80F48Encoder()],
      ["programFeeRate", getWrappedI80F48Encoder()],
      ["panicState", getPanicStateEncoder()],
      ["placeholder1", getU64Encoder()],
      ["liquidationFlatSolFee", getU32Encoder()],
      ["orderInitFlatSolFee", getU32Encoder()],
      ["orderExecutionMaxFee", getWrappedI80F48Encoder()],
      ["pauseDelegateAdmin", getAddressEncoder()],
      ["reserved0", getArrayEncoder(getU64Encoder(), { size: 32 })],
    ]),
    (value) => ({ ...value, discriminator: FEE_STATE_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link FeeState} account data. */
export function getFeeStateDecoder(): FixedSizeDecoder<FeeState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["key", getAddressDecoder()],
    ["globalFeeAdmin", getAddressDecoder()],
    ["globalFeeWallet", getAddressDecoder()],
    ["accountTransferFee", getU32Decoder()],
    ["placeholder0", fixDecoderSize(getBytesDecoder(), 4)],
    ["bankInitFlatSolFee", getU32Decoder()],
    ["bumpSeed", getU8Decoder()],
    ["padding0", fixDecoderSize(getBytesDecoder(), 3)],
    ["liquidationMaxFee", getWrappedI80F48Decoder()],
    ["programFeeFixed", getWrappedI80F48Decoder()],
    ["programFeeRate", getWrappedI80F48Decoder()],
    ["panicState", getPanicStateDecoder()],
    ["placeholder1", getU64Decoder()],
    ["liquidationFlatSolFee", getU32Decoder()],
    ["orderInitFlatSolFee", getU32Decoder()],
    ["orderExecutionMaxFee", getWrappedI80F48Decoder()],
    ["pauseDelegateAdmin", getAddressDecoder()],
    ["reserved0", getArrayDecoder(getU64Decoder(), { size: 32 })],
  ]);
}

/** Gets the codec for {@link FeeState} account data. */
export function getFeeStateCodec(): FixedSizeCodec<FeeStateArgs, FeeState> {
  return combineCodec(getFeeStateEncoder(), getFeeStateDecoder());
}

export function decodeFeeState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<FeeState, TAddress>;
export function decodeFeeState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<FeeState, TAddress>;
export function decodeFeeState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<FeeState, TAddress> | MaybeAccount<FeeState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getFeeStateDecoder());
}

export async function fetchFeeState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<FeeState, TAddress>> {
  const maybeAccount = await fetchMaybeFeeState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeFeeState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<FeeState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeFeeState(maybeAccount);
}

export async function fetchAllFeeState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<FeeState>[]> {
  const maybeAccounts = await fetchAllMaybeFeeState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeFeeState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<FeeState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeFeeState(maybeAccount));
}

export function getFeeStateSize(): number {
  return 520;
}
