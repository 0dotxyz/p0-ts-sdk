/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getHealthCacheDecoder,
  getHealthCacheEncoder,
  getIndexerFlagsDecoder,
  getIndexerFlagsEncoder,
  getLendingAccountDecoder,
  getLendingAccountEncoder,
  type HealthCache,
  type HealthCacheArgs,
  type IndexerFlags,
  type IndexerFlagsArgs,
  type LendingAccount,
  type LendingAccountArgs,
} from "../types";

export const MARGINFI_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  67, 178, 130, 109, 126, 114, 28, 42,
]);

export function getMarginfiAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MARGINFI_ACCOUNT_DISCRIMINATOR);
}

export type MarginfiAccount = {
  discriminator: ReadonlyUint8Array;
  group: Address;
  authority: Address;
  lendingAccount: LendingAccount;
  /**
   * The flags that indicate the state of the account. This is u64 bitfield, where each bit
   * represents a flag.
   *
   * Flags:MarginfiAccount
   * - 1: `ACCOUNT_DISABLED` - Indicates that the account is disabled and no further actions can
   * be taken on it.
   * - 2: `ACCOUNT_IN_FLASHLOAN` - Only set when an account is within a flash loan, e.g. when
   * start_flashloan is called, then unset when the flashloan ends.
   * - 4: `ACCOUNT_FLAG_DEPRECATED` - Deprecated, available for future use
   * - 8: `ACCOUNT_TRANSFER_AUTHORITY_DEPRECATED` - the admin has flagged with account to be
   * moved, original owner can now call `set_account_transfer_authority`
   * - 16: `ACCOUNT_IN_RECEIVERSHIP` - the account is eligible to be liquidated and has entered
   * receivership, a liquidator is able to control borrows and withdraws until the end of the
   * tx. This flag will only appear within a tx.
   * - 32: `ACCOUNT_IN_DELEVERAGE - the account is being deleveraged by the risk admin
   * - 64: `ACCOUNT_FROZEN` - the admin has frozen the account; only the group admin may perform
   * actions until unfrozen.
   */
  accountFlags: bigint;
  /** Wallet whose canonical ATA receives off-chain emissions distributions. */
  emissionsDestinationAccount: Address;
  healthCache: HealthCache;
  /** If this account was migrated from another one, store the original account key */
  migratedFrom: Address;
  /** If this account has been migrated to another one, store the destination account key */
  migratedTo: Address;
  /** Unix timestamp (u64) of the last account interaction. Note: Bank.last_update uses i64. */
  lastUpdate: bigint;
  /**
   * If a PDA-based account, the account index, a seed used to derive the PDA that can be chosen
   * arbitrarily (0.1.5 or later). Otherwise, does nothing.
   */
  accountIndex: number;
  /**
   * If a PDA-based account (0.1.5 or later), a "vendor specific" id. Values < PDA_FREE_THRESHOLD
   * can be used by anyone with no restrictions. Values >= PDA_FREE_THRESHOLD can only be used by
   * a particular program via CPI. These values require being added to a list, contact us for
   * more details. For legacy non-pda accounts, does nothing.
   *
   * Note: use a unique seed to tag accounts related to some particular program or campaign so
   * you can easily fetch them all later.
   */
  thirdPartyIndex: number;
  /** This account's bump, if a PDA-based account (0.1.5 or later). Otherwise, does nothing. */
  bump: number;
  /**
   * Count of how many Orders this account has active. One is added when an Order is opened, and
   * subtracted when an Order is executed or cancelled.
   * * Accounts cannot open more than u8::MAX orders. Sorry power users: hopefully 256 stop
   * losses is enough for you.
   */
  activeOrders: number;
  pad0: ReadonlyUint8Array;
  /**
   * Stores information related to liquidations made against this account. A pda of this
   * account's key, and "liq_record"
   * * Typically pubkey default if this account has never been liquidated or close to liquidation
   * * Opening this account is permissionless. Typically the liquidator pays, but e.g. we may
   * also charge the user if they are opening a risky position on the front end.
   */
  liquidationRecord: Address;
  indexerFlags: IndexerFlags;
  padding0: Array<bigint>;
};

export type MarginfiAccountArgs = {
  group: Address;
  authority: Address;
  lendingAccount: LendingAccountArgs;
  /**
   * The flags that indicate the state of the account. This is u64 bitfield, where each bit
   * represents a flag.
   *
   * Flags:MarginfiAccount
   * - 1: `ACCOUNT_DISABLED` - Indicates that the account is disabled and no further actions can
   * be taken on it.
   * - 2: `ACCOUNT_IN_FLASHLOAN` - Only set when an account is within a flash loan, e.g. when
   * start_flashloan is called, then unset when the flashloan ends.
   * - 4: `ACCOUNT_FLAG_DEPRECATED` - Deprecated, available for future use
   * - 8: `ACCOUNT_TRANSFER_AUTHORITY_DEPRECATED` - the admin has flagged with account to be
   * moved, original owner can now call `set_account_transfer_authority`
   * - 16: `ACCOUNT_IN_RECEIVERSHIP` - the account is eligible to be liquidated and has entered
   * receivership, a liquidator is able to control borrows and withdraws until the end of the
   * tx. This flag will only appear within a tx.
   * - 32: `ACCOUNT_IN_DELEVERAGE - the account is being deleveraged by the risk admin
   * - 64: `ACCOUNT_FROZEN` - the admin has frozen the account; only the group admin may perform
   * actions until unfrozen.
   */
  accountFlags: number | bigint;
  /** Wallet whose canonical ATA receives off-chain emissions distributions. */
  emissionsDestinationAccount: Address;
  healthCache: HealthCacheArgs;
  /** If this account was migrated from another one, store the original account key */
  migratedFrom: Address;
  /** If this account has been migrated to another one, store the destination account key */
  migratedTo: Address;
  /** Unix timestamp (u64) of the last account interaction. Note: Bank.last_update uses i64. */
  lastUpdate: number | bigint;
  /**
   * If a PDA-based account, the account index, a seed used to derive the PDA that can be chosen
   * arbitrarily (0.1.5 or later). Otherwise, does nothing.
   */
  accountIndex: number;
  /**
   * If a PDA-based account (0.1.5 or later), a "vendor specific" id. Values < PDA_FREE_THRESHOLD
   * can be used by anyone with no restrictions. Values >= PDA_FREE_THRESHOLD can only be used by
   * a particular program via CPI. These values require being added to a list, contact us for
   * more details. For legacy non-pda accounts, does nothing.
   *
   * Note: use a unique seed to tag accounts related to some particular program or campaign so
   * you can easily fetch them all later.
   */
  thirdPartyIndex: number;
  /** This account's bump, if a PDA-based account (0.1.5 or later). Otherwise, does nothing. */
  bump: number;
  /**
   * Count of how many Orders this account has active. One is added when an Order is opened, and
   * subtracted when an Order is executed or cancelled.
   * * Accounts cannot open more than u8::MAX orders. Sorry power users: hopefully 256 stop
   * losses is enough for you.
   */
  activeOrders: number;
  pad0: ReadonlyUint8Array;
  /**
   * Stores information related to liquidations made against this account. A pda of this
   * account's key, and "liq_record"
   * * Typically pubkey default if this account has never been liquidated or close to liquidation
   * * Opening this account is permissionless. Typically the liquidator pays, but e.g. we may
   * also charge the user if they are opening a risky position on the front end.
   */
  liquidationRecord: Address;
  indexerFlags: IndexerFlagsArgs;
  padding0: Array<number | bigint>;
};

/** Gets the encoder for {@link MarginfiAccountArgs} account data. */
export function getMarginfiAccountEncoder(): FixedSizeEncoder<MarginfiAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["group", getAddressEncoder()],
      ["authority", getAddressEncoder()],
      ["lendingAccount", getLendingAccountEncoder()],
      ["accountFlags", getU64Encoder()],
      ["emissionsDestinationAccount", getAddressEncoder()],
      ["healthCache", getHealthCacheEncoder()],
      ["migratedFrom", getAddressEncoder()],
      ["migratedTo", getAddressEncoder()],
      ["lastUpdate", getU64Encoder()],
      ["accountIndex", getU16Encoder()],
      ["thirdPartyIndex", getU16Encoder()],
      ["bump", getU8Encoder()],
      ["activeOrders", getU8Encoder()],
      ["pad0", fixEncoderSize(getBytesEncoder(), 2)],
      ["liquidationRecord", getAddressEncoder()],
      ["indexerFlags", getIndexerFlagsEncoder()],
      ["padding0", getArrayEncoder(getU64Encoder(), { size: 4 })],
    ]),
    (value) => ({ ...value, discriminator: MARGINFI_ACCOUNT_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link MarginfiAccount} account data. */
export function getMarginfiAccountDecoder(): FixedSizeDecoder<MarginfiAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["group", getAddressDecoder()],
    ["authority", getAddressDecoder()],
    ["lendingAccount", getLendingAccountDecoder()],
    ["accountFlags", getU64Decoder()],
    ["emissionsDestinationAccount", getAddressDecoder()],
    ["healthCache", getHealthCacheDecoder()],
    ["migratedFrom", getAddressDecoder()],
    ["migratedTo", getAddressDecoder()],
    ["lastUpdate", getU64Decoder()],
    ["accountIndex", getU16Decoder()],
    ["thirdPartyIndex", getU16Decoder()],
    ["bump", getU8Decoder()],
    ["activeOrders", getU8Decoder()],
    ["pad0", fixDecoderSize(getBytesDecoder(), 2)],
    ["liquidationRecord", getAddressDecoder()],
    ["indexerFlags", getIndexerFlagsDecoder()],
    ["padding0", getArrayDecoder(getU64Decoder(), { size: 4 })],
  ]);
}

/** Gets the codec for {@link MarginfiAccount} account data. */
export function getMarginfiAccountCodec(): FixedSizeCodec<MarginfiAccountArgs, MarginfiAccount> {
  return combineCodec(getMarginfiAccountEncoder(), getMarginfiAccountDecoder());
}

export function decodeMarginfiAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<MarginfiAccount, TAddress>;
export function decodeMarginfiAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<MarginfiAccount, TAddress>;
export function decodeMarginfiAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<MarginfiAccount, TAddress> | MaybeAccount<MarginfiAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getMarginfiAccountDecoder()
  );
}

export async function fetchMarginfiAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<MarginfiAccount, TAddress>> {
  const maybeAccount = await fetchMaybeMarginfiAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMarginfiAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<MarginfiAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMarginfiAccount(maybeAccount);
}

export async function fetchAllMarginfiAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<MarginfiAccount>[]> {
  const maybeAccounts = await fetchAllMaybeMarginfiAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMarginfiAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<MarginfiAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMarginfiAccount(maybeAccount));
}

export function getMarginfiAccountSize(): number {
  return 2312;
}
