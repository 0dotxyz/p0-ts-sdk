/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findMetadataPda } from "../pdas";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const INIT_BANK_METADATA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  94, 239, 50, 136, 137, 204, 254, 213,
]);

export function getInitBankMetadataDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_BANK_METADATA_DISCRIMINATOR);
}

export type InitBankMetadataInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountBank extends string | AccountMeta<string> = string,
  TAccountFeePayer extends string | AccountMeta<string> = string,
  TAccountMetadata extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBank extends string ? ReadonlyAccount<TAccountBank> : TAccountBank,
      TAccountFeePayer extends string
        ? WritableSignerAccount<TAccountFeePayer> & AccountSignerMeta<TAccountFeePayer>
        : TAccountFeePayer,
      TAccountMetadata extends string ? WritableAccount<TAccountMetadata> : TAccountMetadata,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitBankMetadataInstructionData = { discriminator: ReadonlyUint8Array };

export type InitBankMetadataInstructionDataArgs = {};

export function getInitBankMetadataInstructionDataEncoder(): FixedSizeEncoder<InitBankMetadataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INIT_BANK_METADATA_DISCRIMINATOR })
  );
}

export function getInitBankMetadataInstructionDataDecoder(): FixedSizeDecoder<InitBankMetadataInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getInitBankMetadataInstructionDataCodec(): FixedSizeCodec<
  InitBankMetadataInstructionDataArgs,
  InitBankMetadataInstructionData
> {
  return combineCodec(
    getInitBankMetadataInstructionDataEncoder(),
    getInitBankMetadataInstructionDataDecoder()
  );
}

export type InitBankMetadataAsyncInput<
  TAccountBank extends InstructionAccountInput = InstructionAccountInput,
  TAccountFeePayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountMetadata extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** keypair-based bank. When initialized with a known seed, the PDA is verified in the handler. */
  bank: TAccountBank;
  /** Pays the init fee */
  feePayer: TAccountFeePayer;
  /** Note: unique per-bank. */
  metadata?: TAccountMetadata;
  systemProgram?: TAccountSystemProgram;
};

export async function getInitBankMetadataInstructionAsync<
  TAccountBank extends InstructionAccountInput,
  TAccountFeePayer extends InstructionSignerInput,
  TAccountMetadata extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: InitBankMetadataAsyncInput<
    TAccountBank,
    TAccountFeePayer,
    TAccountMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitBankMetadataInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountBank, InstructionAccountInputAddress<TAccountBank>>,
    ResolvedInstructionAccountMeta<
      TAccountFeePayer,
      InstructionAccountInputAddress<TAccountFeePayer>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMetadata,
      InstructionAccountInputAddress<TAccountMetadata>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    bank: { value: input.bank ?? null, isSigner: false, isWritable: false },
    feePayer: { value: input.feePayer ?? null, isSigner: true, isWritable: true },
    metadata: { value: input.metadata ?? null, isSigner: false, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.metadata.value) {
    accounts.metadata.value = await findMetadataPda(
      { bank: getAddressFromResolvedInstructionAccount("bank", accounts.bank.value) },
      { programAddress }
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("bank", accounts.bank),
      getAccountMeta("feePayer", accounts.feePayer),
      getAccountMeta("metadata", accounts.metadata),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitBankMetadataInstructionDataEncoder().encode({}),
    programAddress,
  } as InitBankMetadataInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountBank, InstructionAccountInputAddress<TAccountBank>>,
    ResolvedInstructionAccountMeta<
      TAccountFeePayer,
      InstructionAccountInputAddress<TAccountFeePayer>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMetadata,
      InstructionAccountInputAddress<TAccountMetadata>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type InitBankMetadataInput<
  TAccountBank extends InstructionAccountInput = InstructionAccountInput,
  TAccountFeePayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountMetadata extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** keypair-based bank. When initialized with a known seed, the PDA is verified in the handler. */
  bank: TAccountBank;
  /** Pays the init fee */
  feePayer: TAccountFeePayer;
  /** Note: unique per-bank. */
  metadata: TAccountMetadata;
  systemProgram?: TAccountSystemProgram;
};

export function getInitBankMetadataInstruction<
  TAccountBank extends InstructionAccountInput,
  TAccountFeePayer extends InstructionSignerInput,
  TAccountMetadata extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: InitBankMetadataInput<
    TAccountBank,
    TAccountFeePayer,
    TAccountMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitBankMetadataInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountBank, InstructionAccountInputAddress<TAccountBank>>,
  ResolvedInstructionAccountMeta<
    TAccountFeePayer,
    InstructionAccountInputAddress<TAccountFeePayer>
  >,
  ResolvedInstructionAccountMeta<
    TAccountMetadata,
    InstructionAccountInputAddress<TAccountMetadata>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    bank: { value: input.bank ?? null, isSigner: false, isWritable: false },
    feePayer: { value: input.feePayer ?? null, isSigner: true, isWritable: true },
    metadata: { value: input.metadata ?? null, isSigner: false, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("bank", accounts.bank),
      getAccountMeta("feePayer", accounts.feePayer),
      getAccountMeta("metadata", accounts.metadata),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitBankMetadataInstructionDataEncoder().encode({}),
    programAddress,
  } as InitBankMetadataInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountBank, InstructionAccountInputAddress<TAccountBank>>,
    ResolvedInstructionAccountMeta<
      TAccountFeePayer,
      InstructionAccountInputAddress<TAccountFeePayer>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMetadata,
      InstructionAccountInputAddress<TAccountMetadata>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedInitBankMetadataInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** keypair-based bank. When initialized with a known seed, the PDA is verified in the handler. */
    bank: TAccountMetas[0];
    /** Pays the init fee */
    feePayer: TAccountMetas[1];
    /** Note: unique per-bank. */
    metadata: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitBankMetadataInstructionData;
};

export function parseInitBankMetadataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitBankMetadataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bank: getNextAccount(),
      feePayer: getNextAccount(),
      metadata: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitBankMetadataInstructionDataDecoder().decode(instruction.data),
  };
}
