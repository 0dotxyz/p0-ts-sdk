/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const SYNC_INDEXER_FLAGS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  171, 146, 145, 43, 190, 175, 9, 32,
]);

export function getSyncIndexerFlagsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SYNC_INDEXER_FLAGS_DISCRIMINATOR);
}

export type SyncIndexerFlagsInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type SyncIndexerFlagsInstructionData = { discriminator: ReadonlyUint8Array };

export type SyncIndexerFlagsInstructionDataArgs = {};

export function getSyncIndexerFlagsInstructionDataEncoder(): FixedSizeEncoder<SyncIndexerFlagsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SYNC_INDEXER_FLAGS_DISCRIMINATOR })
  );
}

export function getSyncIndexerFlagsInstructionDataDecoder(): FixedSizeDecoder<SyncIndexerFlagsInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getSyncIndexerFlagsInstructionDataCodec(): FixedSizeCodec<
  SyncIndexerFlagsInstructionDataArgs,
  SyncIndexerFlagsInstructionData
> {
  return combineCodec(
    getSyncIndexerFlagsInstructionDataEncoder(),
    getSyncIndexerFlagsInstructionDataDecoder()
  );
}

export type SyncIndexerFlagsInput<
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
> = {
  payer: TAccountPayer;
};

export function getSyncIndexerFlagsInstruction<
  TAccountPayer extends InstructionSignerInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: SyncIndexerFlagsInput<TAccountPayer>,
  config?: { programAddress?: TProgramAddress }
): SyncIndexerFlagsInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isSigner: true, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [getAccountMeta("payer", accounts.payer)],
    data: getSyncIndexerFlagsInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncIndexerFlagsInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>
  >);
}

export type ParsedSyncIndexerFlagsInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
  };
  data: SyncIndexerFlagsInstructionData;
};

export function parseSyncIndexerFlagsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSyncIndexerFlagsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 1,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payer: getNextAccount() },
    data: getSyncIndexerFlagsInstructionDataDecoder().decode(instruction.data),
  };
}
