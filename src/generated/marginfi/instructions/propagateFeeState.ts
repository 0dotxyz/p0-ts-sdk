/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findFeeStatePda } from "../pdas";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const PROPAGATE_FEE_STATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  64, 3, 166, 194, 129, 21, 101, 155,
]);

export function getPropagateFeeStateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PROPAGATE_FEE_STATE_DISCRIMINATOR);
}

export type PropagateFeeStateInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountFeeState extends string | AccountMeta<string> = string,
  TAccountMarginfiGroup extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFeeState extends string ? ReadonlyAccount<TAccountFeeState> : TAccountFeeState,
      TAccountMarginfiGroup extends string
        ? WritableAccount<TAccountMarginfiGroup>
        : TAccountMarginfiGroup,
      ...TRemainingAccounts,
    ]
  >;

export type PropagateFeeStateInstructionData = { discriminator: ReadonlyUint8Array };

export type PropagateFeeStateInstructionDataArgs = {};

export function getPropagateFeeStateInstructionDataEncoder(): FixedSizeEncoder<PropagateFeeStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PROPAGATE_FEE_STATE_DISCRIMINATOR })
  );
}

export function getPropagateFeeStateInstructionDataDecoder(): FixedSizeDecoder<PropagateFeeStateInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getPropagateFeeStateInstructionDataCodec(): FixedSizeCodec<
  PropagateFeeStateInstructionDataArgs,
  PropagateFeeStateInstructionData
> {
  return combineCodec(
    getPropagateFeeStateInstructionDataEncoder(),
    getPropagateFeeStateInstructionDataDecoder()
  );
}

export type PropagateFeeStateAsyncInput<
  TAccountFeeState extends InstructionAccountInput = InstructionAccountInput,
  TAccountMarginfiGroup extends InstructionAccountInput = InstructionAccountInput,
> = {
  feeState?: TAccountFeeState;
  /** Any group, this ix is permisionless and can propagate the fee to any group */
  marginfiGroup: TAccountMarginfiGroup;
};

export async function getPropagateFeeStateInstructionAsync<
  TAccountFeeState extends InstructionAccountInput,
  TAccountMarginfiGroup extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: PropagateFeeStateAsyncInput<TAccountFeeState, TAccountMarginfiGroup>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  PropagateFeeStateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMarginfiGroup,
      InstructionAccountInputAddress<TAccountMarginfiGroup>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    feeState: { value: input.feeState ?? null, isSigner: false, isWritable: false },
    marginfiGroup: { value: input.marginfiGroup ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.feeState.value) {
    accounts.feeState.value = await findFeeStatePda({ programAddress });
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("feeState", accounts.feeState),
      getAccountMeta("marginfiGroup", accounts.marginfiGroup),
    ],
    data: getPropagateFeeStateInstructionDataEncoder().encode({}),
    programAddress,
  } as PropagateFeeStateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMarginfiGroup,
      InstructionAccountInputAddress<TAccountMarginfiGroup>
    >
  >);
}

export type PropagateFeeStateInput<
  TAccountFeeState extends InstructionAccountInput = InstructionAccountInput,
  TAccountMarginfiGroup extends InstructionAccountInput = InstructionAccountInput,
> = {
  feeState: TAccountFeeState;
  /** Any group, this ix is permisionless and can propagate the fee to any group */
  marginfiGroup: TAccountMarginfiGroup;
};

export function getPropagateFeeStateInstruction<
  TAccountFeeState extends InstructionAccountInput,
  TAccountMarginfiGroup extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: PropagateFeeStateInput<TAccountFeeState, TAccountMarginfiGroup>,
  config?: { programAddress?: TProgramAddress }
): PropagateFeeStateInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountFeeState,
    InstructionAccountInputAddress<TAccountFeeState>
  >,
  ResolvedInstructionAccountMeta<
    TAccountMarginfiGroup,
    InstructionAccountInputAddress<TAccountMarginfiGroup>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    feeState: { value: input.feeState ?? null, isSigner: false, isWritable: false },
    marginfiGroup: { value: input.marginfiGroup ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [
      getAccountMeta("feeState", accounts.feeState),
      getAccountMeta("marginfiGroup", accounts.marginfiGroup),
    ],
    data: getPropagateFeeStateInstructionDataEncoder().encode({}),
    programAddress,
  } as PropagateFeeStateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMarginfiGroup,
      InstructionAccountInputAddress<TAccountMarginfiGroup>
    >
  >);
}

export type ParsedPropagateFeeStateInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    feeState: TAccountMetas[0];
    /** Any group, this ix is permisionless and can propagate the fee to any group */
    marginfiGroup: TAccountMetas[1];
  };
  data: PropagateFeeStateInstructionData;
};

export function parsePropagateFeeStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPropagateFeeStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { feeState: getNextAccount(), marginfiGroup: getNextAccount() },
    data: getPropagateFeeStateInstructionDataDecoder().decode(instruction.data),
  };
}
