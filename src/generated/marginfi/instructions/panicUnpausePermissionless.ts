/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findFeeStatePda } from "../pdas";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const PANIC_UNPAUSE_PERMISSIONLESS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  245, 139, 50, 159, 213, 62, 91, 248,
]);

export function getPanicUnpausePermissionlessDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PANIC_UNPAUSE_PERMISSIONLESS_DISCRIMINATOR);
}

export type PanicUnpausePermissionlessInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountFeeState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFeeState extends string ? WritableAccount<TAccountFeeState> : TAccountFeeState,
      ...TRemainingAccounts,
    ]
  >;

export type PanicUnpausePermissionlessInstructionData = { discriminator: ReadonlyUint8Array };

export type PanicUnpausePermissionlessInstructionDataArgs = {};

export function getPanicUnpausePermissionlessInstructionDataEncoder(): FixedSizeEncoder<PanicUnpausePermissionlessInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PANIC_UNPAUSE_PERMISSIONLESS_DISCRIMINATOR })
  );
}

export function getPanicUnpausePermissionlessInstructionDataDecoder(): FixedSizeDecoder<PanicUnpausePermissionlessInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getPanicUnpausePermissionlessInstructionDataCodec(): FixedSizeCodec<
  PanicUnpausePermissionlessInstructionDataArgs,
  PanicUnpausePermissionlessInstructionData
> {
  return combineCodec(
    getPanicUnpausePermissionlessInstructionDataEncoder(),
    getPanicUnpausePermissionlessInstructionDataDecoder()
  );
}

export type PanicUnpausePermissionlessAsyncInput<
  TAccountFeeState extends InstructionAccountInput = InstructionAccountInput,
> = {
  feeState?: TAccountFeeState;
};

export async function getPanicUnpausePermissionlessInstructionAsync<
  TAccountFeeState extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: PanicUnpausePermissionlessAsyncInput<TAccountFeeState>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  PanicUnpausePermissionlessInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    feeState: { value: input.feeState ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.feeState.value) {
    accounts.feeState.value = await findFeeStatePda({ programAddress });
  }

  return Object.freeze({
    accounts: [getAccountMeta("feeState", accounts.feeState)],
    data: getPanicUnpausePermissionlessInstructionDataEncoder().encode({}),
    programAddress,
  } as PanicUnpausePermissionlessInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >
  >);
}

export type PanicUnpausePermissionlessInput<
  TAccountFeeState extends InstructionAccountInput = InstructionAccountInput,
> = {
  feeState: TAccountFeeState;
};

export function getPanicUnpausePermissionlessInstruction<
  TAccountFeeState extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: PanicUnpausePermissionlessInput<TAccountFeeState>,
  config?: { programAddress?: TProgramAddress }
): PanicUnpausePermissionlessInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountFeeState, InstructionAccountInputAddress<TAccountFeeState>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    feeState: { value: input.feeState ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [getAccountMeta("feeState", accounts.feeState)],
    data: getPanicUnpausePermissionlessInstructionDataEncoder().encode({}),
    programAddress,
  } as PanicUnpausePermissionlessInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >
  >);
}

export type ParsedPanicUnpausePermissionlessInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    feeState: TAccountMetas[0];
  };
  data: PanicUnpausePermissionlessInstructionData;
};

export function parsePanicUnpausePermissionlessInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPanicUnpausePermissionlessInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 1,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { feeState: getNextAccount() },
    data: getPanicUnpausePermissionlessInstructionDataDecoder().decode(instruction.data),
  };
}
