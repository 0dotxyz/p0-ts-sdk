/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const LENDING_ACCOUNT_PULSE_HEALTH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  186, 52, 117, 97, 34, 74, 39, 253,
]);

export function getLendingAccountPulseHealthDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LENDING_ACCOUNT_PULSE_HEALTH_DISCRIMINATOR);
}

export type LendingAccountPulseHealthInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMarginfiAccount extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarginfiAccount extends string
        ? WritableAccount<TAccountMarginfiAccount>
        : TAccountMarginfiAccount,
      TAccountGroup extends string ? ReadonlyAccount<TAccountGroup> : TAccountGroup,
      ...TRemainingAccounts,
    ]
  >;

export type LendingAccountPulseHealthInstructionData = { discriminator: ReadonlyUint8Array };

export type LendingAccountPulseHealthInstructionDataArgs = {};

export function getLendingAccountPulseHealthInstructionDataEncoder(): FixedSizeEncoder<LendingAccountPulseHealthInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LENDING_ACCOUNT_PULSE_HEALTH_DISCRIMINATOR })
  );
}

export function getLendingAccountPulseHealthInstructionDataDecoder(): FixedSizeDecoder<LendingAccountPulseHealthInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getLendingAccountPulseHealthInstructionDataCodec(): FixedSizeCodec<
  LendingAccountPulseHealthInstructionDataArgs,
  LendingAccountPulseHealthInstructionData
> {
  return combineCodec(
    getLendingAccountPulseHealthInstructionDataEncoder(),
    getLendingAccountPulseHealthInstructionDataDecoder()
  );
}

export type LendingAccountPulseHealthInput<
  TAccountMarginfiAccount extends InstructionAccountInput = InstructionAccountInput,
  TAccountGroup extends InstructionAccountInput = InstructionAccountInput,
> = {
  marginfiAccount: TAccountMarginfiAccount;
  group: TAccountGroup;
};

export function getLendingAccountPulseHealthInstruction<
  TAccountMarginfiAccount extends InstructionAccountInput,
  TAccountGroup extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: LendingAccountPulseHealthInput<TAccountMarginfiAccount, TAccountGroup>,
  config?: { programAddress?: TProgramAddress }
): LendingAccountPulseHealthInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountMarginfiAccount,
    InstructionAccountInputAddress<TAccountMarginfiAccount>
  >,
  ResolvedInstructionAccountMeta<TAccountGroup, InstructionAccountInputAddress<TAccountGroup>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    marginfiAccount: { value: input.marginfiAccount ?? null, isSigner: false, isWritable: true },
    group: { value: input.group ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [
      getAccountMeta("marginfiAccount", accounts.marginfiAccount),
      getAccountMeta("group", accounts.group),
    ],
    data: getLendingAccountPulseHealthInstructionDataEncoder().encode({}),
    programAddress,
  } as LendingAccountPulseHealthInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountMarginfiAccount,
      InstructionAccountInputAddress<TAccountMarginfiAccount>
    >,
    ResolvedInstructionAccountMeta<TAccountGroup, InstructionAccountInputAddress<TAccountGroup>>
  >);
}

export type ParsedLendingAccountPulseHealthInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    marginfiAccount: TAccountMetas[0];
    group: TAccountMetas[1];
  };
  data: LendingAccountPulseHealthInstructionData;
};

export function parseLendingAccountPulseHealthInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedLendingAccountPulseHealthInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { marginfiAccount: getNextAccount(), group: getNextAccount() },
    data: getLendingAccountPulseHealthInstructionDataDecoder().decode(instruction.data),
  };
}
