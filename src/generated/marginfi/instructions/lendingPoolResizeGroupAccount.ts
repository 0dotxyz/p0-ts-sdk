/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const LENDING_POOL_RESIZE_GROUP_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  97, 221, 69, 96, 204, 162, 174, 250,
]);

export function getLendingPoolResizeGroupAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LENDING_POOL_RESIZE_GROUP_ACCOUNT_DISCRIMINATOR
  );
}

export type LendingPoolResizeGroupAccountInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountGroup extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGroup extends string ? WritableAccount<TAccountGroup> : TAccountGroup,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type LendingPoolResizeGroupAccountInstructionData = { discriminator: ReadonlyUint8Array };

export type LendingPoolResizeGroupAccountInstructionDataArgs = {};

export function getLendingPoolResizeGroupAccountInstructionDataEncoder(): FixedSizeEncoder<LendingPoolResizeGroupAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LENDING_POOL_RESIZE_GROUP_ACCOUNT_DISCRIMINATOR })
  );
}

export function getLendingPoolResizeGroupAccountInstructionDataDecoder(): FixedSizeDecoder<LendingPoolResizeGroupAccountInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getLendingPoolResizeGroupAccountInstructionDataCodec(): FixedSizeCodec<
  LendingPoolResizeGroupAccountInstructionDataArgs,
  LendingPoolResizeGroupAccountInstructionData
> {
  return combineCodec(
    getLendingPoolResizeGroupAccountInstructionDataEncoder(),
    getLendingPoolResizeGroupAccountInstructionDataDecoder()
  );
}

export type LendingPoolResizeGroupAccountInput<
  TAccountGroup extends InstructionAccountInput = InstructionAccountInput,
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** undersized group can still be resized under the future (larger-struct) program. */
  group: TAccountGroup;
  /** Funds the rent for the added account space. */
  payer: TAccountPayer;
  systemProgram?: TAccountSystemProgram;
};

export function getLendingPoolResizeGroupAccountInstruction<
  TAccountGroup extends InstructionAccountInput,
  TAccountPayer extends InstructionSignerInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: LendingPoolResizeGroupAccountInput<TAccountGroup, TAccountPayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): LendingPoolResizeGroupAccountInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountGroup, InstructionAccountInputAddress<TAccountGroup>>,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    group: { value: input.group ?? null, isSigner: false, isWritable: true },
    payer: { value: input.payer ?? null, isSigner: true, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("group", accounts.group),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getLendingPoolResizeGroupAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as LendingPoolResizeGroupAccountInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountGroup, InstructionAccountInputAddress<TAccountGroup>>,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedLendingPoolResizeGroupAccountInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** undersized group can still be resized under the future (larger-struct) program. */
    group: TAccountMetas[0];
    /** Funds the rent for the added account space. */
    payer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: LendingPoolResizeGroupAccountInstructionData;
};

export function parseLendingPoolResizeGroupAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedLendingPoolResizeGroupAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { group: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() },
    data: getLendingPoolResizeGroupAccountInstructionDataDecoder().decode(instruction.data),
  };
}
