/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findFeeStatePda } from "../pdas";
import { MARGINFI_PROGRAM_ADDRESS } from "../programs";

export const RESIZE_GLOBAL_FEE_STATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  141, 111, 97, 79, 111, 143, 77, 159,
]);

export function getResizeGlobalFeeStateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RESIZE_GLOBAL_FEE_STATE_DISCRIMINATOR);
}

export type ResizeGlobalFeeStateInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountFeeState extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFeeState extends string ? WritableAccount<TAccountFeeState> : TAccountFeeState,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ResizeGlobalFeeStateInstructionData = { discriminator: ReadonlyUint8Array };

export type ResizeGlobalFeeStateInstructionDataArgs = {};

export function getResizeGlobalFeeStateInstructionDataEncoder(): FixedSizeEncoder<ResizeGlobalFeeStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESIZE_GLOBAL_FEE_STATE_DISCRIMINATOR })
  );
}

export function getResizeGlobalFeeStateInstructionDataDecoder(): FixedSizeDecoder<ResizeGlobalFeeStateInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getResizeGlobalFeeStateInstructionDataCodec(): FixedSizeCodec<
  ResizeGlobalFeeStateInstructionDataArgs,
  ResizeGlobalFeeStateInstructionData
> {
  return combineCodec(
    getResizeGlobalFeeStateInstructionDataEncoder(),
    getResizeGlobalFeeStateInstructionDataDecoder()
  );
}

export type ResizeGlobalFeeStateAsyncInput<
  TAccountFeeState extends InstructionAccountInput = InstructionAccountInput,
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /**
   * Not an AccountLoader so an undersized fee state can still be resized under the future
   * (larger-struct) program.
   */
  feeState?: TAccountFeeState;
  /** Funds the rent for the added account space. */
  payer: TAccountPayer;
  systemProgram?: TAccountSystemProgram;
};

export async function getResizeGlobalFeeStateInstructionAsync<
  TAccountFeeState extends InstructionAccountInput,
  TAccountPayer extends InstructionSignerInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: ResizeGlobalFeeStateAsyncInput<TAccountFeeState, TAccountPayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ResizeGlobalFeeStateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    feeState: { value: input.feeState ?? null, isSigner: false, isWritable: true },
    payer: { value: input.payer ?? null, isSigner: true, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.feeState.value) {
    accounts.feeState.value = await findFeeStatePda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("feeState", accounts.feeState),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getResizeGlobalFeeStateInstructionDataEncoder().encode({}),
    programAddress,
  } as ResizeGlobalFeeStateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ResizeGlobalFeeStateInput<
  TAccountFeeState extends InstructionAccountInput = InstructionAccountInput,
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /**
   * Not an AccountLoader so an undersized fee state can still be resized under the future
   * (larger-struct) program.
   */
  feeState: TAccountFeeState;
  /** Funds the rent for the added account space. */
  payer: TAccountPayer;
  systemProgram?: TAccountSystemProgram;
};

export function getResizeGlobalFeeStateInstruction<
  TAccountFeeState extends InstructionAccountInput,
  TAccountPayer extends InstructionSignerInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MARGINFI_PROGRAM_ADDRESS,
>(
  input: ResizeGlobalFeeStateInput<TAccountFeeState, TAccountPayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): ResizeGlobalFeeStateInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountFeeState,
    InstructionAccountInputAddress<TAccountFeeState>
  >,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARGINFI_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    feeState: { value: input.feeState ?? null, isSigner: false, isWritable: true },
    payer: { value: input.payer ?? null, isSigner: true, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("feeState", accounts.feeState),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getResizeGlobalFeeStateInstructionDataEncoder().encode({}),
    programAddress,
  } as ResizeGlobalFeeStateInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFeeState,
      InstructionAccountInputAddress<TAccountFeeState>
    >,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedResizeGlobalFeeStateInstruction<
  TProgram extends string = typeof MARGINFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Not an AccountLoader so an undersized fee state can still be resized under the future
     * (larger-struct) program.
     */
    feeState: TAccountMetas[0];
    /** Funds the rent for the added account space. */
    payer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: ResizeGlobalFeeStateInstructionData;
};

export function parseResizeGlobalFeeStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedResizeGlobalFeeStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      feeState: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getResizeGlobalFeeStateInstructionDataDecoder().decode(instruction.data),
  };
}
