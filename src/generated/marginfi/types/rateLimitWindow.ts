/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * A sliding window rate limiter that tracks net outflow over a time window.
 * Uses weighted blend of previous and current windows for smooth transitions.
 *
 * Net outflow = (withdraws + borrows) - (deposits + repays).
 * A negative net outflow increases remaining capacity for subsequent outflows.
 */
export type RateLimitWindow = {
  /**
   * Maximum net outflow allowed per window (0 = disabled).
   * For bank-level: denominated in native tokens.
   * For group-level: denominated in USD.
   */
  maxOutflow: bigint;
  /** Window duration in seconds (e.g., 3600 for hourly, 86400 for daily). */
  windowDuration: bigint;
  /** Unix timestamp when the current window started. */
  windowStart: bigint;
  /**
   * Net outflow accumulated in the previous window.
   * Signed to allow tracking when inflows exceed outflows.
   */
  prevWindowOutflow: bigint;
  /**
   * Net outflow accumulated in the current window.
   * Signed to allow tracking when inflows exceed outflows.
   */
  curWindowOutflow: bigint;
};

export type RateLimitWindowArgs = {
  /**
   * Maximum net outflow allowed per window (0 = disabled).
   * For bank-level: denominated in native tokens.
   * For group-level: denominated in USD.
   */
  maxOutflow: number | bigint;
  /** Window duration in seconds (e.g., 3600 for hourly, 86400 for daily). */
  windowDuration: number | bigint;
  /** Unix timestamp when the current window started. */
  windowStart: number | bigint;
  /**
   * Net outflow accumulated in the previous window.
   * Signed to allow tracking when inflows exceed outflows.
   */
  prevWindowOutflow: number | bigint;
  /**
   * Net outflow accumulated in the current window.
   * Signed to allow tracking when inflows exceed outflows.
   */
  curWindowOutflow: number | bigint;
};

export function getRateLimitWindowEncoder(): FixedSizeEncoder<RateLimitWindowArgs> {
  return getStructEncoder([
    ["maxOutflow", getU64Encoder()],
    ["windowDuration", getU64Encoder()],
    ["windowStart", getI64Encoder()],
    ["prevWindowOutflow", getI64Encoder()],
    ["curWindowOutflow", getI64Encoder()],
  ]);
}

export function getRateLimitWindowDecoder(): FixedSizeDecoder<RateLimitWindow> {
  return getStructDecoder([
    ["maxOutflow", getU64Decoder()],
    ["windowDuration", getU64Decoder()],
    ["windowStart", getI64Decoder()],
    ["prevWindowOutflow", getI64Decoder()],
    ["curWindowOutflow", getI64Decoder()],
  ]);
}

export function getRateLimitWindowCodec(): FixedSizeCodec<RateLimitWindowArgs, RateLimitWindow> {
  return combineCodec(getRateLimitWindowEncoder(), getRateLimitWindowDecoder());
}
