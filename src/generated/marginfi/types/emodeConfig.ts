/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getEmodeEntryDecoder,
  getEmodeEntryEncoder,
  type EmodeEntry,
  type EmodeEntryArgs,
} from ".";

/**
 * An emode configuration. Each bank has one such configuration, but this may also be the
 * intersection of many configurations (see `reconcile_emode_configs`). For example, the risk
 * engine creates such an intersection from all the emode config of all banks the user is borrowing
 * from.
 */
export type EmodeConfig = { entries: Array<EmodeEntry> };

export type EmodeConfigArgs = { entries: Array<EmodeEntryArgs> };

export function getEmodeConfigEncoder(): FixedSizeEncoder<EmodeConfigArgs> {
  return getStructEncoder([["entries", getArrayEncoder(getEmodeEntryEncoder(), { size: 10 })]]);
}

export function getEmodeConfigDecoder(): FixedSizeDecoder<EmodeConfig> {
  return getStructDecoder([["entries", getArrayDecoder(getEmodeEntryDecoder(), { size: 10 })]]);
}

export function getEmodeConfigCodec(): FixedSizeCodec<EmodeConfigArgs, EmodeConfig> {
  return combineCodec(getEmodeConfigEncoder(), getEmodeConfigDecoder());
}
