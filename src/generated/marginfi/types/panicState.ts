/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/** Panic state for emergency protocol pausing */
export type PanicState = {
  /** Whether the protocol is currently paused (1 = paused, 0 = not paused) */
  pauseFlags: number;
  /** Number of times paused today (resets every 24 hours) */
  dailyPauseCount: number;
  /** Number of consecutive pauses (resets when unpause happens) */
  consecutivePauseCount: number;
  reserved: ReadonlyUint8Array;
  /**
   * Timestamp when the current pause started (0 if not paused)
   * * When a pause is extended before expiring, this could be in the future.
   */
  pauseStartTimestamp: bigint;
  /** Timestamp of the last daily reset (for tracking daily pause count) */
  lastDailyResetTimestamp: bigint;
  /** Reserved for future use (making total struct 32 bytes) */
  reservedSpace: ReadonlyUint8Array;
};

export type PanicStateArgs = {
  /** Whether the protocol is currently paused (1 = paused, 0 = not paused) */
  pauseFlags: number;
  /** Number of times paused today (resets every 24 hours) */
  dailyPauseCount: number;
  /** Number of consecutive pauses (resets when unpause happens) */
  consecutivePauseCount: number;
  reserved: ReadonlyUint8Array;
  /**
   * Timestamp when the current pause started (0 if not paused)
   * * When a pause is extended before expiring, this could be in the future.
   */
  pauseStartTimestamp: number | bigint;
  /** Timestamp of the last daily reset (for tracking daily pause count) */
  lastDailyResetTimestamp: number | bigint;
  /** Reserved for future use (making total struct 32 bytes) */
  reservedSpace: ReadonlyUint8Array;
};

export function getPanicStateEncoder(): FixedSizeEncoder<PanicStateArgs> {
  return getStructEncoder([
    ["pauseFlags", getU8Encoder()],
    ["dailyPauseCount", getU8Encoder()],
    ["consecutivePauseCount", getU8Encoder()],
    ["reserved", fixEncoderSize(getBytesEncoder(), 5)],
    ["pauseStartTimestamp", getI64Encoder()],
    ["lastDailyResetTimestamp", getI64Encoder()],
    ["reservedSpace", fixEncoderSize(getBytesEncoder(), 8)],
  ]);
}

export function getPanicStateDecoder(): FixedSizeDecoder<PanicState> {
  return getStructDecoder([
    ["pauseFlags", getU8Decoder()],
    ["dailyPauseCount", getU8Decoder()],
    ["consecutivePauseCount", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 5)],
    ["pauseStartTimestamp", getI64Decoder()],
    ["lastDailyResetTimestamp", getI64Decoder()],
    ["reservedSpace", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPanicStateCodec(): FixedSizeCodec<PanicStateArgs, PanicState> {
  return combineCodec(getPanicStateEncoder(), getPanicStateDecoder());
}
