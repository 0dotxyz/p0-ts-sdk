/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getEmodeConfigDecoder,
  getEmodeConfigEncoder,
  type EmodeConfig,
  type EmodeConfigArgs,
} from ".";

/**
 * Controls the bank's e-mode configuration, allowing certain collateral sources to be treated more
 * favorably as collateral when used to borrow from this bank.
 */
export type EmodeSettings = {
  /**
   * This bank's NON-unique id that other banks will use to determine what emode rate to use when
   * this bank is offered as collateral.
   *
   * For example, all stablecoin banks might share the same emode_tag, and in their entries, each
   * such stablecoin bank will recognize that collateral sources with this "stable" tag get
   * preferential weights. When a new stablecoin is added that is considered riskier, it may get
   * a new, less favorable emode tag, and eventually get upgraded to the same one as the other
   * stables
   *
   * * 0 is in an invalid tag and will do nothing.
   */
  emodeTag: number;
  pad0: ReadonlyUint8Array;
  /** Unix timestamp from the system clock when emode state was last updated */
  timestamp: bigint;
  /**
   * EMODE_ON (1) - If set, at least one entry is configured
   * 2, 4, 8, etc, Reserved for future use
   */
  flags: bigint;
  emodeConfig: EmodeConfig;
};

export type EmodeSettingsArgs = {
  /**
   * This bank's NON-unique id that other banks will use to determine what emode rate to use when
   * this bank is offered as collateral.
   *
   * For example, all stablecoin banks might share the same emode_tag, and in their entries, each
   * such stablecoin bank will recognize that collateral sources with this "stable" tag get
   * preferential weights. When a new stablecoin is added that is considered riskier, it may get
   * a new, less favorable emode tag, and eventually get upgraded to the same one as the other
   * stables
   *
   * * 0 is in an invalid tag and will do nothing.
   */
  emodeTag: number;
  pad0: ReadonlyUint8Array;
  /** Unix timestamp from the system clock when emode state was last updated */
  timestamp: number | bigint;
  /**
   * EMODE_ON (1) - If set, at least one entry is configured
   * 2, 4, 8, etc, Reserved for future use
   */
  flags: number | bigint;
  emodeConfig: EmodeConfigArgs;
};

export function getEmodeSettingsEncoder(): FixedSizeEncoder<EmodeSettingsArgs> {
  return getStructEncoder([
    ["emodeTag", getU16Encoder()],
    ["pad0", fixEncoderSize(getBytesEncoder(), 6)],
    ["timestamp", getI64Encoder()],
    ["flags", getU64Encoder()],
    ["emodeConfig", getEmodeConfigEncoder()],
  ]);
}

export function getEmodeSettingsDecoder(): FixedSizeDecoder<EmodeSettings> {
  return getStructDecoder([
    ["emodeTag", getU16Decoder()],
    ["pad0", fixDecoderSize(getBytesDecoder(), 6)],
    ["timestamp", getI64Decoder()],
    ["flags", getU64Decoder()],
    ["emodeConfig", getEmodeConfigDecoder()],
  ]);
}

export function getEmodeSettingsCodec(): FixedSizeCodec<EmodeSettingsArgs, EmodeSettings> {
  return combineCodec(getEmodeSettingsEncoder(), getEmodeSettingsDecoder());
}
