/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getRateLimitWindowDecoder,
  getRateLimitWindowEncoder,
  type RateLimitWindow,
  type RateLimitWindowArgs,
} from ".";

/**
 * Per-bank rate limiting configuration and state.
 * Tracks net outflow in native tokens.
 */
export type BankRateLimiter = {
  /** Hourly window rate limiter (native tokens). */
  hourly: RateLimitWindow;
  /** Daily window rate limiter (native tokens). */
  daily: RateLimitWindow;
};

export type BankRateLimiterArgs = {
  /** Hourly window rate limiter (native tokens). */
  hourly: RateLimitWindowArgs;
  /** Daily window rate limiter (native tokens). */
  daily: RateLimitWindowArgs;
};

export function getBankRateLimiterEncoder(): FixedSizeEncoder<BankRateLimiterArgs> {
  return getStructEncoder([
    ["hourly", getRateLimitWindowEncoder()],
    ["daily", getRateLimitWindowEncoder()],
  ]);
}

export function getBankRateLimiterDecoder(): FixedSizeDecoder<BankRateLimiter> {
  return getStructDecoder([
    ["hourly", getRateLimitWindowDecoder()],
    ["daily", getRateLimitWindowDecoder()],
  ]);
}

export function getBankRateLimiterCodec(): FixedSizeCodec<BankRateLimiterArgs, BankRateLimiter> {
  return combineCodec(getBankRateLimiterEncoder(), getBankRateLimiterDecoder());
}
