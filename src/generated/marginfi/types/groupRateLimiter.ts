/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getRateLimitWindowDecoder,
  getRateLimitWindowEncoder,
  type RateLimitWindow,
  type RateLimitWindowArgs,
} from ".";

/**
 * Per-group rate limiting configuration and state.
 * Tracks aggregate net outflow in USD.
 */
export type GroupRateLimiter = {
  /** Hourly window rate limiter (USD). */
  hourly: RateLimitWindow;
  /** Daily window rate limiter (USD). */
  daily: RateLimitWindow;
};

export type GroupRateLimiterArgs = {
  /** Hourly window rate limiter (USD). */
  hourly: RateLimitWindowArgs;
  /** Daily window rate limiter (USD). */
  daily: RateLimitWindowArgs;
};

export function getGroupRateLimiterEncoder(): FixedSizeEncoder<GroupRateLimiterArgs> {
  return getStructEncoder([
    ["hourly", getRateLimitWindowEncoder()],
    ["daily", getRateLimitWindowEncoder()],
  ]);
}

export function getGroupRateLimiterDecoder(): FixedSizeDecoder<GroupRateLimiter> {
  return getStructDecoder([
    ["hourly", getRateLimitWindowDecoder()],
    ["daily", getRateLimitWindowDecoder()],
  ]);
}

export function getGroupRateLimiterCodec(): FixedSizeCodec<GroupRateLimiterArgs, GroupRateLimiter> {
  return combineCodec(getGroupRateLimiterEncoder(), getGroupRateLimiterDecoder());
}
