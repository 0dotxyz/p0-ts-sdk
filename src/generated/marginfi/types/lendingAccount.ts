/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { getBalanceDecoder, getBalanceEncoder, type Balance, type BalanceArgs } from ".";

/** The lending account holds up to 16 balance positions for a user. */
export type LendingAccount = {
  /** Array of balance positions (max 16). Sorted in descending order by bank_pk. */
  balances: Array<Balance>;
  /** Last allocated balance tag (u16), used to find the next unused tag. */
  lastTagUsed: number;
  /** Reserved for future use */
  pad1: ReadonlyUint8Array;
  /** Reserved for future use */
  padding: Array<bigint>;
};

export type LendingAccountArgs = {
  /** Array of balance positions (max 16). Sorted in descending order by bank_pk. */
  balances: Array<BalanceArgs>;
  /** Last allocated balance tag (u16), used to find the next unused tag. */
  lastTagUsed: number;
  /** Reserved for future use */
  pad1: ReadonlyUint8Array;
  /** Reserved for future use */
  padding: Array<number | bigint>;
};

export function getLendingAccountEncoder(): FixedSizeEncoder<LendingAccountArgs> {
  return getStructEncoder([
    ["balances", getArrayEncoder(getBalanceEncoder(), { size: 16 })],
    ["lastTagUsed", getU16Encoder()],
    ["pad1", fixEncoderSize(getBytesEncoder(), 6)],
    ["padding", getArrayEncoder(getU64Encoder(), { size: 7 })],
  ]);
}

export function getLendingAccountDecoder(): FixedSizeDecoder<LendingAccount> {
  return getStructDecoder([
    ["balances", getArrayDecoder(getBalanceDecoder(), { size: 16 })],
    ["lastTagUsed", getU16Decoder()],
    ["pad1", fixDecoderSize(getBytesDecoder(), 6)],
    ["padding", getArrayDecoder(getU64Decoder(), { size: 7 })],
  ]);
}

export function getLendingAccountCodec(): FixedSizeCodec<LendingAccountArgs, LendingAccount> {
  return combineCodec(getLendingAccountEncoder(), getLendingAccountDecoder());
}
