/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findGlobalConfigPda } from "../pdas";
import { GAMMA_VAULT_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ADMIN_AUTHORITIES_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  41, 241, 142, 184, 75, 212, 72, 171,
]);

export function getUpdateAdminAuthoritiesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_ADMIN_AUTHORITIES_DISCRIMINATOR);
}

export type UpdateAdminAuthoritiesInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> =
    "GaMmanX9i4jGmqDZZD2tbD6B2v9p21btenPneMXnTczV",
  TAccountProgramData extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountGlobalConfig extends string
        ? WritableAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram,
      TAccountProgramData extends string
        ? ReadonlyAccount<TAccountProgramData>
        : TAccountProgramData,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAdminAuthoritiesInstructionData = {
  discriminator: ReadonlyUint8Array;
  newSuperAdmin: Address;
  newCreateAuthority: Address;
  newPermissionedCreation: boolean;
};

export type UpdateAdminAuthoritiesInstructionDataArgs = {
  newSuperAdmin: Address;
  newCreateAuthority: Address;
  newPermissionedCreation: boolean;
};

export function getUpdateAdminAuthoritiesInstructionDataEncoder(): FixedSizeEncoder<UpdateAdminAuthoritiesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newSuperAdmin", getAddressEncoder()],
      ["newCreateAuthority", getAddressEncoder()],
      ["newPermissionedCreation", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ADMIN_AUTHORITIES_DISCRIMINATOR })
  );
}

export function getUpdateAdminAuthoritiesInstructionDataDecoder(): FixedSizeDecoder<UpdateAdminAuthoritiesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newSuperAdmin", getAddressDecoder()],
    ["newCreateAuthority", getAddressDecoder()],
    ["newPermissionedCreation", getBooleanDecoder()],
  ]);
}

export function getUpdateAdminAuthoritiesInstructionDataCodec(): FixedSizeCodec<
  UpdateAdminAuthoritiesInstructionDataArgs,
  UpdateAdminAuthoritiesInstructionData
> {
  return combineCodec(
    getUpdateAdminAuthoritiesInstructionDataEncoder(),
    getUpdateAdminAuthoritiesInstructionDataDecoder()
  );
}

export type UpdateAdminAuthoritiesAsyncInput<
  TAccountSigner extends InstructionSignerInput = InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput = InstructionAccountInput,
  TAccountProgram extends InstructionAccountInput = InstructionAccountInput,
  TAccountProgramData extends InstructionAccountInput = InstructionAccountInput,
> = {
  /**
   * Signer rotating the authorities. MUST be the program's
   * BPFLoaderUpgradeable upgrade authority (proven by the constraints on
   * `program` + `program_data` below).
   */
  signer: TAccountSigner;
  /**
   * The global config account being rotated. Unique in the program.
   * Deliberately no `has_one = super_admin`: see the struct docs.
   */
  globalConfig?: TAccountGlobalConfig;
  /**
   * This program's own account; ties `program_data` to this program so the
   * upgrade-authority check below cannot be satisfied with an unrelated
   * program's ProgramData.
   */
  program?: TAccountProgram;
  /**
   * ProgramData account holding the program's upgrade authority. Gate: the
   * signer must equal the upgrade authority. `programdata_address()` returns
   * `Ok(None)` for a non-upgradeable load and a revoked authority is `None`,
   * so a `None` on either side fails closed.
   */
  programData: TAccountProgramData;
  newSuperAdmin: UpdateAdminAuthoritiesInstructionDataArgs["newSuperAdmin"];
  newCreateAuthority: UpdateAdminAuthoritiesInstructionDataArgs["newCreateAuthority"];
  newPermissionedCreation: UpdateAdminAuthoritiesInstructionDataArgs["newPermissionedCreation"];
};

export async function getUpdateAdminAuthoritiesInstructionAsync<
  TAccountSigner extends InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput,
  TAccountProgram extends InstructionAccountInput,
  TAccountProgramData extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateAdminAuthoritiesAsyncInput<
    TAccountSigner,
    TAccountGlobalConfig,
    TAccountProgram,
    TAccountProgramData
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateAdminAuthoritiesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
    ResolvedInstructionAccountMeta<
      TAccountGlobalConfig,
      InstructionAccountInputAddress<TAccountGlobalConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProgram,
      InstructionAccountInputAddress<TAccountProgram>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProgramData,
      InstructionAccountInputAddress<TAccountProgramData>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isSigner: true, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isSigner: false, isWritable: true },
    program: { value: input.program ?? null, isSigner: false, isWritable: false },
    programData: { value: input.programData ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda({ programAddress });
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "GaMmanX9i4jGmqDZZD2tbD6B2v9p21btenPneMXnTczV" as Address<"GaMmanX9i4jGmqDZZD2tbD6B2v9p21btenPneMXnTczV">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("program", accounts.program),
      getAccountMeta("programData", accounts.programData),
    ],
    data: getUpdateAdminAuthoritiesInstructionDataEncoder().encode(
      args as UpdateAdminAuthoritiesInstructionDataArgs
    ),
    programAddress,
  } as UpdateAdminAuthoritiesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
    ResolvedInstructionAccountMeta<
      TAccountGlobalConfig,
      InstructionAccountInputAddress<TAccountGlobalConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProgram,
      InstructionAccountInputAddress<TAccountProgram>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProgramData,
      InstructionAccountInputAddress<TAccountProgramData>
    >
  >);
}

export type UpdateAdminAuthoritiesInput<
  TAccountSigner extends InstructionSignerInput = InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput = InstructionAccountInput,
  TAccountProgram extends InstructionAccountInput = InstructionAccountInput,
  TAccountProgramData extends InstructionAccountInput = InstructionAccountInput,
> = {
  /**
   * Signer rotating the authorities. MUST be the program's
   * BPFLoaderUpgradeable upgrade authority (proven by the constraints on
   * `program` + `program_data` below).
   */
  signer: TAccountSigner;
  /**
   * The global config account being rotated. Unique in the program.
   * Deliberately no `has_one = super_admin`: see the struct docs.
   */
  globalConfig: TAccountGlobalConfig;
  /**
   * This program's own account; ties `program_data` to this program so the
   * upgrade-authority check below cannot be satisfied with an unrelated
   * program's ProgramData.
   */
  program?: TAccountProgram;
  /**
   * ProgramData account holding the program's upgrade authority. Gate: the
   * signer must equal the upgrade authority. `programdata_address()` returns
   * `Ok(None)` for a non-upgradeable load and a revoked authority is `None`,
   * so a `None` on either side fails closed.
   */
  programData: TAccountProgramData;
  newSuperAdmin: UpdateAdminAuthoritiesInstructionDataArgs["newSuperAdmin"];
  newCreateAuthority: UpdateAdminAuthoritiesInstructionDataArgs["newCreateAuthority"];
  newPermissionedCreation: UpdateAdminAuthoritiesInstructionDataArgs["newPermissionedCreation"];
};

export function getUpdateAdminAuthoritiesInstruction<
  TAccountSigner extends InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput,
  TAccountProgram extends InstructionAccountInput,
  TAccountProgramData extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateAdminAuthoritiesInput<
    TAccountSigner,
    TAccountGlobalConfig,
    TAccountProgram,
    TAccountProgramData
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateAdminAuthoritiesInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
  ResolvedInstructionAccountMeta<
    TAccountGlobalConfig,
    InstructionAccountInputAddress<TAccountGlobalConfig>
  >,
  ResolvedInstructionAccountMeta<TAccountProgram, InstructionAccountInputAddress<TAccountProgram>>,
  ResolvedInstructionAccountMeta<
    TAccountProgramData,
    InstructionAccountInputAddress<TAccountProgramData>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isSigner: true, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isSigner: false, isWritable: true },
    program: { value: input.program ?? null, isSigner: false, isWritable: false },
    programData: { value: input.programData ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.program.value) {
    accounts.program.value =
      "GaMmanX9i4jGmqDZZD2tbD6B2v9p21btenPneMXnTczV" as Address<"GaMmanX9i4jGmqDZZD2tbD6B2v9p21btenPneMXnTczV">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("program", accounts.program),
      getAccountMeta("programData", accounts.programData),
    ],
    data: getUpdateAdminAuthoritiesInstructionDataEncoder().encode(
      args as UpdateAdminAuthoritiesInstructionDataArgs
    ),
    programAddress,
  } as UpdateAdminAuthoritiesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
    ResolvedInstructionAccountMeta<
      TAccountGlobalConfig,
      InstructionAccountInputAddress<TAccountGlobalConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProgram,
      InstructionAccountInputAddress<TAccountProgram>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProgramData,
      InstructionAccountInputAddress<TAccountProgramData>
    >
  >);
}

export type ParsedUpdateAdminAuthoritiesInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Signer rotating the authorities. MUST be the program's
     * BPFLoaderUpgradeable upgrade authority (proven by the constraints on
     * `program` + `program_data` below).
     */
    signer: TAccountMetas[0];
    /**
     * The global config account being rotated. Unique in the program.
     * Deliberately no `has_one = super_admin`: see the struct docs.
     */
    globalConfig: TAccountMetas[1];
    /**
     * This program's own account; ties `program_data` to this program so the
     * upgrade-authority check below cannot be satisfied with an unrelated
     * program's ProgramData.
     */
    program: TAccountMetas[2];
    /**
     * ProgramData account holding the program's upgrade authority. Gate: the
     * signer must equal the upgrade authority. `programdata_address()` returns
     * `Ok(None)` for a non-upgradeable load and a revoked authority is `None`,
     * so a `None` on either side fails closed.
     */
    programData: TAccountMetas[3];
  };
  data: UpdateAdminAuthoritiesInstructionData;
};

export function parseUpdateAdminAuthoritiesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateAdminAuthoritiesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      globalConfig: getNextAccount(),
      program: getNextAccount(),
      programData: getNextAccount(),
    },
    data: getUpdateAdminAuthoritiesInstructionDataDecoder().decode(instruction.data),
  };
}
