/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { GAMMA_VAULT_PROGRAM_ADDRESS } from "../programs";

export const SET_KEEPER_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  29, 124, 191, 114, 184, 243, 33, 155,
]);

export function getSetKeeperAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_KEEPER_AUTHORITY_DISCRIMINATOR);
}

export type SetKeeperAuthorityInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountFundAuthority extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountLpVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFundAuthority extends string
        ? ReadonlySignerAccount<TAccountFundAuthority> & AccountSignerMeta<TAccountFundAuthority>
        : TAccountFundAuthority,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountLpVault extends string ? WritableAccount<TAccountLpVault> : TAccountLpVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetKeeperAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  keeperAuthority: Address;
};

export type SetKeeperAuthorityInstructionDataArgs = { keeperAuthority: Address };

export function getSetKeeperAuthorityInstructionDataEncoder(): FixedSizeEncoder<SetKeeperAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["keeperAuthority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_KEEPER_AUTHORITY_DISCRIMINATOR })
  );
}

export function getSetKeeperAuthorityInstructionDataDecoder(): FixedSizeDecoder<SetKeeperAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["keeperAuthority", getAddressDecoder()],
  ]);
}

export function getSetKeeperAuthorityInstructionDataCodec(): FixedSizeCodec<
  SetKeeperAuthorityInstructionDataArgs,
  SetKeeperAuthorityInstructionData
> {
  return combineCodec(
    getSetKeeperAuthorityInstructionDataEncoder(),
    getSetKeeperAuthorityInstructionDataDecoder()
  );
}

export type SetKeeperAuthorityInput<
  TAccountFundAuthority extends InstructionSignerInput = InstructionSignerInput,
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** Fund authority — only they can change keeper authority. */
  fundAuthority: TAccountFundAuthority;
  /** Payer for any reallocation rent. */
  payer: TAccountPayer;
  lpVault: TAccountLpVault;
  systemProgram?: TAccountSystemProgram;
  keeperAuthority: SetKeeperAuthorityInstructionDataArgs["keeperAuthority"];
};

export function getSetKeeperAuthorityInstruction<
  TAccountFundAuthority extends InstructionSignerInput,
  TAccountPayer extends InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: SetKeeperAuthorityInput<
    TAccountFundAuthority,
    TAccountPayer,
    TAccountLpVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SetKeeperAuthorityInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountFundAuthority,
    InstructionAccountInputAddress<TAccountFundAuthority>
  >,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
  ResolvedInstructionAccountMeta<TAccountLpVault, InstructionAccountInputAddress<TAccountLpVault>>,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    fundAuthority: { value: input.fundAuthority ?? null, isSigner: true, isWritable: false },
    payer: { value: input.payer ?? null, isSigner: true, isWritable: true },
    lpVault: { value: input.lpVault ?? null, isSigner: false, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("fundAuthority", accounts.fundAuthority),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("lpVault", accounts.lpVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSetKeeperAuthorityInstructionDataEncoder().encode(
      args as SetKeeperAuthorityInstructionDataArgs
    ),
    programAddress,
  } as SetKeeperAuthorityInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFundAuthority,
      InstructionAccountInputAddress<TAccountFundAuthority>
    >,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<
      TAccountLpVault,
      InstructionAccountInputAddress<TAccountLpVault>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedSetKeeperAuthorityInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Fund authority — only they can change keeper authority. */
    fundAuthority: TAccountMetas[0];
    /** Payer for any reallocation rent. */
    payer: TAccountMetas[1];
    lpVault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SetKeeperAuthorityInstructionData;
};

export function parseSetKeeperAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetKeeperAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      fundAuthority: getNextAccount(),
      payer: getNextAccount(),
      lpVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetKeeperAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
