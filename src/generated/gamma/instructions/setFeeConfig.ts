/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { GAMMA_VAULT_PROGRAM_ADDRESS } from "../programs";

export const SET_FEE_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  221, 222, 52, 206, 114, 198, 64, 91,
]);

export function getSetFeeConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_FEE_CONFIG_DISCRIMINATOR);
}

export type SetFeeConfigInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountFundAuthority extends string | AccountMeta<string> = string,
  TAccountLpVault extends string | AccountMeta<string> = string,
  TAccountSharesMint extends string | AccountMeta<string> = string,
  TAccountFeeRecipientAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFundAuthority extends string
        ? ReadonlySignerAccount<TAccountFundAuthority> & AccountSignerMeta<TAccountFundAuthority>
        : TAccountFundAuthority,
      TAccountLpVault extends string ? WritableAccount<TAccountLpVault> : TAccountLpVault,
      TAccountSharesMint extends string ? WritableAccount<TAccountSharesMint> : TAccountSharesMint,
      TAccountFeeRecipientAccount extends string
        ? WritableAccount<TAccountFeeRecipientAccount>
        : TAccountFeeRecipientAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetFeeConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  performanceFeeBps: number;
  assessmentIntervalSecs: bigint;
};

export type SetFeeConfigInstructionDataArgs = {
  performanceFeeBps: number;
  assessmentIntervalSecs: number | bigint;
};

export function getSetFeeConfigInstructionDataEncoder(): FixedSizeEncoder<SetFeeConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["performanceFeeBps", getU16Encoder()],
      ["assessmentIntervalSecs", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_FEE_CONFIG_DISCRIMINATOR })
  );
}

export function getSetFeeConfigInstructionDataDecoder(): FixedSizeDecoder<SetFeeConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["performanceFeeBps", getU16Decoder()],
    ["assessmentIntervalSecs", getI64Decoder()],
  ]);
}

export function getSetFeeConfigInstructionDataCodec(): FixedSizeCodec<
  SetFeeConfigInstructionDataArgs,
  SetFeeConfigInstructionData
> {
  return combineCodec(
    getSetFeeConfigInstructionDataEncoder(),
    getSetFeeConfigInstructionDataDecoder()
  );
}

export type SetFeeConfigInput<
  TAccountFundAuthority extends InstructionSignerInput = InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput = InstructionAccountInput,
  TAccountSharesMint extends InstructionAccountInput = InstructionAccountInput,
  TAccountFeeRecipientAccount extends InstructionAccountInput = InstructionAccountInput,
  TAccountTokenProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** Operator / fund authority. */
  fundAuthority: TAccountFundAuthority;
  lpVault: TAccountLpVault;
  sharesMint: TAccountSharesMint;
  /**
   * Token account that will receive fee shares.
   * Required when enabling fees (performance_fee_bps > 0) or when
   * mid-window settlement is needed (accrued fees must be settled
   * to the old recipient before changing config).
   * Omit when disabling with no accrued fees.
   * Mutable because it may be the destination of a MintTo CPI during mid-window settlement.
   */
  feeRecipientAccount?: TAccountFeeRecipientAccount;
  tokenProgram?: TAccountTokenProgram;
  performanceFeeBps: SetFeeConfigInstructionDataArgs["performanceFeeBps"];
  assessmentIntervalSecs: SetFeeConfigInstructionDataArgs["assessmentIntervalSecs"];
};

export function getSetFeeConfigInstruction<
  TAccountFundAuthority extends InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput,
  TAccountSharesMint extends InstructionAccountInput,
  TAccountFeeRecipientAccount extends InstructionAccountInput,
  TAccountTokenProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: SetFeeConfigInput<
    TAccountFundAuthority,
    TAccountLpVault,
    TAccountSharesMint,
    TAccountFeeRecipientAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SetFeeConfigInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountFundAuthority,
    InstructionAccountInputAddress<TAccountFundAuthority>
  >,
  ResolvedInstructionAccountMeta<TAccountLpVault, InstructionAccountInputAddress<TAccountLpVault>>,
  ResolvedInstructionAccountMeta<
    TAccountSharesMint,
    InstructionAccountInputAddress<TAccountSharesMint>
  >,
  ResolvedInstructionAccountMeta<
    TAccountFeeRecipientAccount,
    InstructionAccountInputAddress<TAccountFeeRecipientAccount>
  >,
  ResolvedInstructionAccountMeta<
    TAccountTokenProgram,
    InstructionAccountInputAddress<TAccountTokenProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    fundAuthority: { value: input.fundAuthority ?? null, isSigner: true, isWritable: false },
    lpVault: { value: input.lpVault ?? null, isSigner: false, isWritable: true },
    sharesMint: { value: input.sharesMint ?? null, isSigner: false, isWritable: true },
    feeRecipientAccount: {
      value: input.feeRecipientAccount ?? null,
      isSigner: false,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("fundAuthority", accounts.fundAuthority),
      getAccountMeta("lpVault", accounts.lpVault),
      getAccountMeta("sharesMint", accounts.sharesMint),
      getAccountMeta("feeRecipientAccount", accounts.feeRecipientAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getSetFeeConfigInstructionDataEncoder().encode(args as SetFeeConfigInstructionDataArgs),
    programAddress,
  } as SetFeeConfigInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountFundAuthority,
      InstructionAccountInputAddress<TAccountFundAuthority>
    >,
    ResolvedInstructionAccountMeta<
      TAccountLpVault,
      InstructionAccountInputAddress<TAccountLpVault>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSharesMint,
      InstructionAccountInputAddress<TAccountSharesMint>
    >,
    ResolvedInstructionAccountMeta<
      TAccountFeeRecipientAccount,
      InstructionAccountInputAddress<TAccountFeeRecipientAccount>
    >,
    ResolvedInstructionAccountMeta<
      TAccountTokenProgram,
      InstructionAccountInputAddress<TAccountTokenProgram>
    >
  >);
}

export type ParsedSetFeeConfigInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Operator / fund authority. */
    fundAuthority: TAccountMetas[0];
    lpVault: TAccountMetas[1];
    sharesMint: TAccountMetas[2];
    /**
     * Token account that will receive fee shares.
     * Required when enabling fees (performance_fee_bps > 0) or when
     * mid-window settlement is needed (accrued fees must be settled
     * to the old recipient before changing config).
     * Omit when disabling with no accrued fees.
     * Mutable because it may be the destination of a MintTo CPI during mid-window settlement.
     */
    feeRecipientAccount?: TAccountMetas[3] | undefined;
    tokenProgram: TAccountMetas[4];
  };
  data: SetFeeConfigInstructionData;
};

export function parseSetFeeConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetFeeConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GAMMA_VAULT_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      fundAuthority: getNextAccount(),
      lpVault: getNextAccount(),
      sharesMint: getNextAccount(),
      feeRecipientAccount: getNextOptionalAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getSetFeeConfigInstructionDataDecoder().decode(instruction.data),
  };
}
