/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findGlobalConfigPda } from "../pdas";
import { GAMMA_VAULT_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_GLOBAL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  164, 84, 130, 189, 111, 58, 250, 200,
]);

export function getUpdateGlobalConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_GLOBAL_CONFIG_DISCRIMINATOR);
}

export type UpdateGlobalConfigInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountSuperAdmin extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSuperAdmin extends string
        ? WritableSignerAccount<TAccountSuperAdmin> & AccountSignerMeta<TAccountSuperAdmin>
        : TAccountSuperAdmin,
      TAccountGlobalConfig extends string
        ? WritableAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateGlobalConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  newCreateAuthority: Address;
  isCreationPermissioned: boolean;
};

export type UpdateGlobalConfigInstructionDataArgs = {
  newCreateAuthority: Address;
  isCreationPermissioned: boolean;
};

export function getUpdateGlobalConfigInstructionDataEncoder(): FixedSizeEncoder<UpdateGlobalConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newCreateAuthority", getAddressEncoder()],
      ["isCreationPermissioned", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_GLOBAL_CONFIG_DISCRIMINATOR })
  );
}

export function getUpdateGlobalConfigInstructionDataDecoder(): FixedSizeDecoder<UpdateGlobalConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newCreateAuthority", getAddressDecoder()],
    ["isCreationPermissioned", getBooleanDecoder()],
  ]);
}

export function getUpdateGlobalConfigInstructionDataCodec(): FixedSizeCodec<
  UpdateGlobalConfigInstructionDataArgs,
  UpdateGlobalConfigInstructionData
> {
  return combineCodec(
    getUpdateGlobalConfigInstructionDataEncoder(),
    getUpdateGlobalConfigInstructionDataDecoder()
  );
}

export type UpdateGlobalConfigAsyncInput<
  TAccountSuperAdmin extends InstructionSignerInput = InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** The signer must be the global_config super_admin. */
  superAdmin: TAccountSuperAdmin;
  /** The global config account. Unique in the program. */
  globalConfig?: TAccountGlobalConfig;
  newCreateAuthority: UpdateGlobalConfigInstructionDataArgs["newCreateAuthority"];
  isCreationPermissioned: UpdateGlobalConfigInstructionDataArgs["isCreationPermissioned"];
};

export async function getUpdateGlobalConfigInstructionAsync<
  TAccountSuperAdmin extends InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateGlobalConfigAsyncInput<TAccountSuperAdmin, TAccountGlobalConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateGlobalConfigInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountSuperAdmin,
      InstructionAccountInputAddress<TAccountSuperAdmin>
    >,
    ResolvedInstructionAccountMeta<
      TAccountGlobalConfig,
      InstructionAccountInputAddress<TAccountGlobalConfig>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isSigner: true, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda({ programAddress });
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("superAdmin", accounts.superAdmin),
      getAccountMeta("globalConfig", accounts.globalConfig),
    ],
    data: getUpdateGlobalConfigInstructionDataEncoder().encode(
      args as UpdateGlobalConfigInstructionDataArgs
    ),
    programAddress,
  } as UpdateGlobalConfigInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountSuperAdmin,
      InstructionAccountInputAddress<TAccountSuperAdmin>
    >,
    ResolvedInstructionAccountMeta<
      TAccountGlobalConfig,
      InstructionAccountInputAddress<TAccountGlobalConfig>
    >
  >);
}

export type UpdateGlobalConfigInput<
  TAccountSuperAdmin extends InstructionSignerInput = InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** The signer must be the global_config super_admin. */
  superAdmin: TAccountSuperAdmin;
  /** The global config account. Unique in the program. */
  globalConfig: TAccountGlobalConfig;
  newCreateAuthority: UpdateGlobalConfigInstructionDataArgs["newCreateAuthority"];
  isCreationPermissioned: UpdateGlobalConfigInstructionDataArgs["isCreationPermissioned"];
};

export function getUpdateGlobalConfigInstruction<
  TAccountSuperAdmin extends InstructionSignerInput,
  TAccountGlobalConfig extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateGlobalConfigInput<TAccountSuperAdmin, TAccountGlobalConfig>,
  config?: { programAddress?: TProgramAddress }
): UpdateGlobalConfigInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountSuperAdmin,
    InstructionAccountInputAddress<TAccountSuperAdmin>
  >,
  ResolvedInstructionAccountMeta<
    TAccountGlobalConfig,
    InstructionAccountInputAddress<TAccountGlobalConfig>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isSigner: true, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("superAdmin", accounts.superAdmin),
      getAccountMeta("globalConfig", accounts.globalConfig),
    ],
    data: getUpdateGlobalConfigInstructionDataEncoder().encode(
      args as UpdateGlobalConfigInstructionDataArgs
    ),
    programAddress,
  } as UpdateGlobalConfigInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountSuperAdmin,
      InstructionAccountInputAddress<TAccountSuperAdmin>
    >,
    ResolvedInstructionAccountMeta<
      TAccountGlobalConfig,
      InstructionAccountInputAddress<TAccountGlobalConfig>
    >
  >);
}

export type ParsedUpdateGlobalConfigInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The signer must be the global_config super_admin. */
    superAdmin: TAccountMetas[0];
    /** The global config account. Unique in the program. */
    globalConfig: TAccountMetas[1];
  };
  data: UpdateGlobalConfigInstructionData;
};

export function parseUpdateGlobalConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateGlobalConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { superAdmin: getNextAccount(), globalConfig: getNextAccount() },
    data: getUpdateGlobalConfigInstructionDataDecoder().decode(instruction.data),
  };
}
