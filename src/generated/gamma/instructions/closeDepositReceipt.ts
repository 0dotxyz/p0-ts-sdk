/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findDepositReceiptPda } from "../pdas";
import { GAMMA_VAULT_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_DEPOSIT_RECEIPT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  216, 104, 127, 60, 88, 217, 184, 15,
]);

export function getCloseDepositReceiptDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_DEPOSIT_RECEIPT_DISCRIMINATOR);
}

export type CloseDepositReceiptInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountLpVault extends string | AccountMeta<string> = string,
  TAccountDepositReceipt extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountLpVault extends string ? ReadonlyAccount<TAccountLpVault> : TAccountLpVault,
      TAccountDepositReceipt extends string
        ? WritableAccount<TAccountDepositReceipt>
        : TAccountDepositReceipt,
      ...TRemainingAccounts,
    ]
  >;

export type CloseDepositReceiptInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseDepositReceiptInstructionDataArgs = {};

export function getCloseDepositReceiptInstructionDataEncoder(): FixedSizeEncoder<CloseDepositReceiptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_DEPOSIT_RECEIPT_DISCRIMINATOR })
  );
}

export function getCloseDepositReceiptInstructionDataDecoder(): FixedSizeDecoder<CloseDepositReceiptInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getCloseDepositReceiptInstructionDataCodec(): FixedSizeCodec<
  CloseDepositReceiptInstructionDataArgs,
  CloseDepositReceiptInstructionData
> {
  return combineCodec(
    getCloseDepositReceiptInstructionDataEncoder(),
    getCloseDepositReceiptInstructionDataDecoder()
  );
}

export type CloseDepositReceiptAsyncInput<
  TAccountUser extends InstructionSignerInput = InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput = InstructionAccountInput,
  TAccountDepositReceipt extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** User who owns the deposit receipt. */
  user: TAccountUser;
  /** The vault this receipt is associated with. */
  lpVault: TAccountLpVault;
  /** Deposit receipt to close. Rent is returned to user. */
  depositReceipt?: TAccountDepositReceipt;
};

export async function getCloseDepositReceiptInstructionAsync<
  TAccountUser extends InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput,
  TAccountDepositReceipt extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: CloseDepositReceiptAsyncInput<TAccountUser, TAccountLpVault, TAccountDepositReceipt>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseDepositReceiptInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
    ResolvedInstructionAccountMeta<
      TAccountLpVault,
      InstructionAccountInputAddress<TAccountLpVault>
    >,
    ResolvedInstructionAccountMeta<
      TAccountDepositReceipt,
      InstructionAccountInputAddress<TAccountDepositReceipt>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isSigner: true, isWritable: true },
    lpVault: { value: input.lpVault ?? null, isSigner: false, isWritable: false },
    depositReceipt: { value: input.depositReceipt ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.depositReceipt.value) {
    accounts.depositReceipt.value = await findDepositReceiptPda(
      {
        user: getAddressFromResolvedInstructionAccount("user", accounts.user.value),
        lpVault: getAddressFromResolvedInstructionAccount("lpVault", accounts.lpVault.value),
      },
      { programAddress }
    );
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("lpVault", accounts.lpVault),
      getAccountMeta("depositReceipt", accounts.depositReceipt),
    ],
    data: getCloseDepositReceiptInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseDepositReceiptInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
    ResolvedInstructionAccountMeta<
      TAccountLpVault,
      InstructionAccountInputAddress<TAccountLpVault>
    >,
    ResolvedInstructionAccountMeta<
      TAccountDepositReceipt,
      InstructionAccountInputAddress<TAccountDepositReceipt>
    >
  >);
}

export type CloseDepositReceiptInput<
  TAccountUser extends InstructionSignerInput = InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput = InstructionAccountInput,
  TAccountDepositReceipt extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** User who owns the deposit receipt. */
  user: TAccountUser;
  /** The vault this receipt is associated with. */
  lpVault: TAccountLpVault;
  /** Deposit receipt to close. Rent is returned to user. */
  depositReceipt: TAccountDepositReceipt;
};

export function getCloseDepositReceiptInstruction<
  TAccountUser extends InstructionSignerInput,
  TAccountLpVault extends InstructionAccountInput,
  TAccountDepositReceipt extends InstructionAccountInput,
  TProgramAddress extends Address = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
>(
  input: CloseDepositReceiptInput<TAccountUser, TAccountLpVault, TAccountDepositReceipt>,
  config?: { programAddress?: TProgramAddress }
): CloseDepositReceiptInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
  ResolvedInstructionAccountMeta<TAccountLpVault, InstructionAccountInputAddress<TAccountLpVault>>,
  ResolvedInstructionAccountMeta<
    TAccountDepositReceipt,
    InstructionAccountInputAddress<TAccountDepositReceipt>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GAMMA_VAULT_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isSigner: true, isWritable: true },
    lpVault: { value: input.lpVault ?? null, isSigner: false, isWritable: false },
    depositReceipt: { value: input.depositReceipt ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("lpVault", accounts.lpVault),
      getAccountMeta("depositReceipt", accounts.depositReceipt),
    ],
    data: getCloseDepositReceiptInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseDepositReceiptInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
    ResolvedInstructionAccountMeta<
      TAccountLpVault,
      InstructionAccountInputAddress<TAccountLpVault>
    >,
    ResolvedInstructionAccountMeta<
      TAccountDepositReceipt,
      InstructionAccountInputAddress<TAccountDepositReceipt>
    >
  >);
}

export type ParsedCloseDepositReceiptInstruction<
  TProgram extends string = typeof GAMMA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** User who owns the deposit receipt. */
    user: TAccountMetas[0];
    /** The vault this receipt is associated with. */
    lpVault: TAccountMetas[1];
    /** Deposit receipt to close. Rent is returned to user. */
    depositReceipt: TAccountMetas[2];
  };
  data: CloseDepositReceiptInstructionData;
};

export function parseCloseDepositReceiptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseDepositReceiptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      lpVault: getNextAccount(),
      depositReceipt: getNextAccount(),
    },
    data: getCloseDepositReceiptInstructionDataDecoder().decode(instruction.data),
  };
}
