/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LP_VAULT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  189, 45, 167, 23, 91, 118, 105, 190,
]);

export function getLpVaultDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LP_VAULT_DISCRIMINATOR);
}

export type LpVault = {
  discriminator: ReadonlyUint8Array;
  /** USDC TokenAccount where users deposit, funds agent operations. */
  assetsAccount: Address;
  /** TokenAccount where shares are held during queued (illiquid) withdrawals. */
  pendingSharesAccount: Address;
  /** Derived mint (created on initialize) that represents LP tokens or shares. */
  sharesMint: Address;
  /** Mint corresponding to the asset, most of the time USDC. */
  assetsMint: Address;
  /** Pubkey authorized to withdraw assets from assets_account and update NAV. */
  fundAuthority: Address;
  /** Current Net Asset Value (total vault value in asset decimals). */
  nav: bigint;
  /**
   * Total shares outstanding (cached for efficiency). May temporarily diverge
   * from shares_mint.supply during the escrow withdrawal flow (between
   * fulfill_withdrawal and complete_withdrawal).
   */
  totalShares: bigint;
  /** Timestamp for when NAV was last updated. */
  navUpdatedAt: bigint;
  /**
   * Maximum NAV staleness for user operations (in seconds).
   * Deposits and withdrawals fail if NAV is older than this.
   */
  navMaxStaleness: bigint;
  /** PDA derivation bump, stored for CU saving purposes. */
  bump: number;
  vaultName: string;
  /**
   * Total value of pending withdrawals awaiting liquidity.
   * When a withdrawal cannot be fulfilled instantly due to insufficient
   * liquid assets, the value is tracked here. This is approximate — the
   * value recorded at withdrawal time may differ from the value at
   * fulfillment time (recomputed at current NAV). Used for operator
   * visibility only, not for critical calculations.
   */
  pendingWithdrawalValue: bigint;
  /** Shares token account that receives fee shares. Pubkey::default() = disabled. */
  feeRecipient: Address;
  /** Performance fee rate in basis points (2000 = 20%). 0 = disabled. */
  performanceFeeBps: number;
  /** How often scheduled fees are assessed (seconds). E.g., 86400 = 24h. */
  assessmentIntervalSecs: bigint;
  /** Unix timestamp of last fee event (scheduled or withdrawal-triggered). */
  lastAssessmentTimestamp: bigint;
  /** PPS at last fee event, scaled by PRICE_SCALE. High-water mark for fees. */
  pricePerShareAtLastAssessment: bigint;
  /**
   * Optional keeper authority. When set (non-default), this key can call
   * fulfill_withdrawal in addition to fund_authority. Allows the keeper
   * process to use a separate key from the agent/fund_authority.
   * Pubkey::default() = not set (only fund_authority can fulfill).
   */
  keeperAuthority: Address;
};

export type LpVaultArgs = {
  /** USDC TokenAccount where users deposit, funds agent operations. */
  assetsAccount: Address;
  /** TokenAccount where shares are held during queued (illiquid) withdrawals. */
  pendingSharesAccount: Address;
  /** Derived mint (created on initialize) that represents LP tokens or shares. */
  sharesMint: Address;
  /** Mint corresponding to the asset, most of the time USDC. */
  assetsMint: Address;
  /** Pubkey authorized to withdraw assets from assets_account and update NAV. */
  fundAuthority: Address;
  /** Current Net Asset Value (total vault value in asset decimals). */
  nav: number | bigint;
  /**
   * Total shares outstanding (cached for efficiency). May temporarily diverge
   * from shares_mint.supply during the escrow withdrawal flow (between
   * fulfill_withdrawal and complete_withdrawal).
   */
  totalShares: number | bigint;
  /** Timestamp for when NAV was last updated. */
  navUpdatedAt: number | bigint;
  /**
   * Maximum NAV staleness for user operations (in seconds).
   * Deposits and withdrawals fail if NAV is older than this.
   */
  navMaxStaleness: number | bigint;
  /** PDA derivation bump, stored for CU saving purposes. */
  bump: number;
  vaultName: string;
  /**
   * Total value of pending withdrawals awaiting liquidity.
   * When a withdrawal cannot be fulfilled instantly due to insufficient
   * liquid assets, the value is tracked here. This is approximate — the
   * value recorded at withdrawal time may differ from the value at
   * fulfillment time (recomputed at current NAV). Used for operator
   * visibility only, not for critical calculations.
   */
  pendingWithdrawalValue: number | bigint;
  /** Shares token account that receives fee shares. Pubkey::default() = disabled. */
  feeRecipient: Address;
  /** Performance fee rate in basis points (2000 = 20%). 0 = disabled. */
  performanceFeeBps: number;
  /** How often scheduled fees are assessed (seconds). E.g., 86400 = 24h. */
  assessmentIntervalSecs: number | bigint;
  /** Unix timestamp of last fee event (scheduled or withdrawal-triggered). */
  lastAssessmentTimestamp: number | bigint;
  /** PPS at last fee event, scaled by PRICE_SCALE. High-water mark for fees. */
  pricePerShareAtLastAssessment: number | bigint;
  /**
   * Optional keeper authority. When set (non-default), this key can call
   * fulfill_withdrawal in addition to fund_authority. Allows the keeper
   * process to use a separate key from the agent/fund_authority.
   * Pubkey::default() = not set (only fund_authority can fulfill).
   */
  keeperAuthority: Address;
};

/** Gets the encoder for {@link LpVaultArgs} account data. */
export function getLpVaultEncoder(): Encoder<LpVaultArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["assetsAccount", getAddressEncoder()],
      ["pendingSharesAccount", getAddressEncoder()],
      ["sharesMint", getAddressEncoder()],
      ["assetsMint", getAddressEncoder()],
      ["fundAuthority", getAddressEncoder()],
      ["nav", getU64Encoder()],
      ["totalShares", getU64Encoder()],
      ["navUpdatedAt", getI64Encoder()],
      ["navMaxStaleness", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["vaultName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["pendingWithdrawalValue", getU64Encoder()],
      ["feeRecipient", getAddressEncoder()],
      ["performanceFeeBps", getU16Encoder()],
      ["assessmentIntervalSecs", getI64Encoder()],
      ["lastAssessmentTimestamp", getI64Encoder()],
      ["pricePerShareAtLastAssessment", getU64Encoder()],
      ["keeperAuthority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: LP_VAULT_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link LpVault} account data. */
export function getLpVaultDecoder(): Decoder<LpVault> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["assetsAccount", getAddressDecoder()],
    ["pendingSharesAccount", getAddressDecoder()],
    ["sharesMint", getAddressDecoder()],
    ["assetsMint", getAddressDecoder()],
    ["fundAuthority", getAddressDecoder()],
    ["nav", getU64Decoder()],
    ["totalShares", getU64Decoder()],
    ["navUpdatedAt", getI64Decoder()],
    ["navMaxStaleness", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["vaultName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["pendingWithdrawalValue", getU64Decoder()],
    ["feeRecipient", getAddressDecoder()],
    ["performanceFeeBps", getU16Decoder()],
    ["assessmentIntervalSecs", getI64Decoder()],
    ["lastAssessmentTimestamp", getI64Decoder()],
    ["pricePerShareAtLastAssessment", getU64Decoder()],
    ["keeperAuthority", getAddressDecoder()],
  ]);
}

/** Gets the codec for {@link LpVault} account data. */
export function getLpVaultCodec(): Codec<LpVaultArgs, LpVault> {
  return combineCodec(getLpVaultEncoder(), getLpVaultDecoder());
}

export function decodeLpVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<LpVault, TAddress>;
export function decodeLpVault<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<LpVault, TAddress>;
export function decodeLpVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<LpVault, TAddress> | MaybeAccount<LpVault, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLpVaultDecoder());
}

export async function fetchLpVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<LpVault, TAddress>> {
  const maybeAccount = await fetchMaybeLpVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLpVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<LpVault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLpVault(maybeAccount);
}

export async function fetchAllLpVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<LpVault>[]> {
  const maybeAccounts = await fetchAllMaybeLpVault(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLpVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<LpVault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLpVault(maybeAccount));
}
