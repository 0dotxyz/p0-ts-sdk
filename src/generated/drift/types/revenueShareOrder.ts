/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getMarketTypeDecoder,
  getMarketTypeEncoder,
  type MarketType,
  type MarketTypeArgs,
} from ".";

export type RevenueShareOrder = {
  /**
   * fees accrued so far for this order slot. This is not exclusively fees from this order_id
   * and may include fees from other orders in the same market. This may be swept to the
   * builder's SpotPosition during settle_pnl.
   */
  feesAccrued: bigint;
  /** the order_id of the current active order in this slot. It's only relevant while bit_flag = Open */
  orderId: number;
  /** the builder fee on this order, in tenths of a bps, e.g. 100 = 0.01% */
  feeTenthBps: number;
  marketIndex: number;
  /** the subaccount_id of the user who created this order. It's only relevant while bit_flag = Open */
  subAccountId: number;
  /**
   * the index of the RevenueShareEscrow.approved_builders list, that this order's fee will settle to. Ignored
   * if bit_flag = Referral.
   */
  builderIdx: number;
  /**
   * bitflags that describe the state of the order.
   * [`RevenueShareOrderBitFlag::Init`]: this order slot is available for use.
   * [`RevenueShareOrderBitFlag::Open`]: this order slot is occupied, `order_id` is the `sub_account_id`'s active order.
   * [`RevenueShareOrderBitFlag::Completed`]: this order has been filled or canceled, and is waiting to be settled into.
   * the builder's account order_id and sub_account_id are no longer relevant, it may be merged with other orders.
   * [`RevenueShareOrderBitFlag::Referral`]: this order stores referral rewards waiting to be settled for this market.
   * If it is set, no other bitflag should be set.
   */
  bitFlags: number;
  /** the index into the User's orders list when this RevenueShareOrder was created, make sure to verify that order_id matches. */
  userOrderIndex: number;
  marketType: MarketType;
  padding: ReadonlyUint8Array;
};

export type RevenueShareOrderArgs = {
  /**
   * fees accrued so far for this order slot. This is not exclusively fees from this order_id
   * and may include fees from other orders in the same market. This may be swept to the
   * builder's SpotPosition during settle_pnl.
   */
  feesAccrued: number | bigint;
  /** the order_id of the current active order in this slot. It's only relevant while bit_flag = Open */
  orderId: number;
  /** the builder fee on this order, in tenths of a bps, e.g. 100 = 0.01% */
  feeTenthBps: number;
  marketIndex: number;
  /** the subaccount_id of the user who created this order. It's only relevant while bit_flag = Open */
  subAccountId: number;
  /**
   * the index of the RevenueShareEscrow.approved_builders list, that this order's fee will settle to. Ignored
   * if bit_flag = Referral.
   */
  builderIdx: number;
  /**
   * bitflags that describe the state of the order.
   * [`RevenueShareOrderBitFlag::Init`]: this order slot is available for use.
   * [`RevenueShareOrderBitFlag::Open`]: this order slot is occupied, `order_id` is the `sub_account_id`'s active order.
   * [`RevenueShareOrderBitFlag::Completed`]: this order has been filled or canceled, and is waiting to be settled into.
   * the builder's account order_id and sub_account_id are no longer relevant, it may be merged with other orders.
   * [`RevenueShareOrderBitFlag::Referral`]: this order stores referral rewards waiting to be settled for this market.
   * If it is set, no other bitflag should be set.
   */
  bitFlags: number;
  /** the index into the User's orders list when this RevenueShareOrder was created, make sure to verify that order_id matches. */
  userOrderIndex: number;
  marketType: MarketTypeArgs;
  padding: ReadonlyUint8Array;
};

export function getRevenueShareOrderEncoder(): FixedSizeEncoder<RevenueShareOrderArgs> {
  return getStructEncoder([
    ["feesAccrued", getU64Encoder()],
    ["orderId", getU32Encoder()],
    ["feeTenthBps", getU16Encoder()],
    ["marketIndex", getU16Encoder()],
    ["subAccountId", getU16Encoder()],
    ["builderIdx", getU8Encoder()],
    ["bitFlags", getU8Encoder()],
    ["userOrderIndex", getU8Encoder()],
    ["marketType", getMarketTypeEncoder()],
    ["padding", fixEncoderSize(getBytesEncoder(), 10)],
  ]);
}

export function getRevenueShareOrderDecoder(): FixedSizeDecoder<RevenueShareOrder> {
  return getStructDecoder([
    ["feesAccrued", getU64Decoder()],
    ["orderId", getU32Decoder()],
    ["feeTenthBps", getU16Decoder()],
    ["marketIndex", getU16Decoder()],
    ["subAccountId", getU16Decoder()],
    ["builderIdx", getU8Decoder()],
    ["bitFlags", getU8Decoder()],
    ["userOrderIndex", getU8Decoder()],
    ["marketType", getMarketTypeDecoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 10)],
  ]);
}

export function getRevenueShareOrderCodec(): FixedSizeCodec<
  RevenueShareOrderArgs,
  RevenueShareOrder
> {
  return combineCodec(getRevenueShareOrderEncoder(), getRevenueShareOrderDecoder());
}
