/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getPreciseNumberDecoder,
  getPreciseNumberEncoder,
  type PreciseNumber,
  type PreciseNumberArgs,
} from ".";

export type EmissionInfo = {
  /** The token account for the emission where the vault authority is the authority */
  tokenAccount: Address;
  initialIndex: PreciseNumber;
  lastSeenIndex: PreciseNumber;
  /** The final index is used to track the last claimable index after the vault expires */
  finalIndex: PreciseNumber;
  /** The treasury token account for this reward */
  treasuryTokenAccount: Address;
  /** The fee taken from emission collecting */
  feeBps: number;
  /** The lambo fund */
  treasuryEmission: bigint;
};

export type EmissionInfoArgs = {
  /** The token account for the emission where the vault authority is the authority */
  tokenAccount: Address;
  initialIndex: PreciseNumberArgs;
  lastSeenIndex: PreciseNumberArgs;
  /** The final index is used to track the last claimable index after the vault expires */
  finalIndex: PreciseNumberArgs;
  /** The treasury token account for this reward */
  treasuryTokenAccount: Address;
  /** The fee taken from emission collecting */
  feeBps: number;
  /** The lambo fund */
  treasuryEmission: number | bigint;
};

export function getEmissionInfoEncoder(): FixedSizeEncoder<EmissionInfoArgs> {
  return getStructEncoder([
    ["tokenAccount", getAddressEncoder()],
    ["initialIndex", getPreciseNumberEncoder()],
    ["lastSeenIndex", getPreciseNumberEncoder()],
    ["finalIndex", getPreciseNumberEncoder()],
    ["treasuryTokenAccount", getAddressEncoder()],
    ["feeBps", getU16Encoder()],
    ["treasuryEmission", getU64Encoder()],
  ]);
}

export function getEmissionInfoDecoder(): FixedSizeDecoder<EmissionInfo> {
  return getStructDecoder([
    ["tokenAccount", getAddressDecoder()],
    ["initialIndex", getPreciseNumberDecoder()],
    ["lastSeenIndex", getPreciseNumberDecoder()],
    ["finalIndex", getPreciseNumberDecoder()],
    ["treasuryTokenAccount", getAddressDecoder()],
    ["feeBps", getU16Decoder()],
    ["treasuryEmission", getU64Decoder()],
  ]);
}

export function getEmissionInfoCodec(): FixedSizeCodec<EmissionInfoArgs, EmissionInfo> {
  return combineCodec(getEmissionInfoEncoder(), getEmissionInfoDecoder());
}
