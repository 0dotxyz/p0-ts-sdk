/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getPreciseNumberDecoder,
  getPreciseNumberEncoder,
  type PreciseNumber,
  type PreciseNumberArgs,
} from ".";

/** Generic tracker for interest and emissions earned by YT deposits */
export type YieldTokenTracker = {
  /**
   * The index is the per-share value of the SY token
   * Note that the YT balance must be converted to the equivalent SY balance
   */
  lastSeenIndex: PreciseNumber;
  /** Staged tokens that may be withdrawn */
  staged: bigint;
};

export type YieldTokenTrackerArgs = {
  /**
   * The index is the per-share value of the SY token
   * Note that the YT balance must be converted to the equivalent SY balance
   */
  lastSeenIndex: PreciseNumberArgs;
  /** Staged tokens that may be withdrawn */
  staged: number | bigint;
};

export function getYieldTokenTrackerEncoder(): FixedSizeEncoder<YieldTokenTrackerArgs> {
  return getStructEncoder([
    ["lastSeenIndex", getPreciseNumberEncoder()],
    ["staged", getU64Encoder()],
  ]);
}

export function getYieldTokenTrackerDecoder(): FixedSizeDecoder<YieldTokenTracker> {
  return getStructDecoder([
    ["lastSeenIndex", getPreciseNumberDecoder()],
    ["staged", getU64Decoder()],
  ]);
}

export function getYieldTokenTrackerCodec(): FixedSizeCodec<
  YieldTokenTrackerArgs,
  YieldTokenTracker
> {
  return combineCodec(getYieldTokenTrackerEncoder(), getYieldTokenTrackerDecoder());
}
