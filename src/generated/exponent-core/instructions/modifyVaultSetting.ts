/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { EXPONENT_CORE_PROGRAM_ADDRESS } from "../programs";
import {
  getAdminActionDecoder,
  getAdminActionEncoder,
  type AdminAction,
  type AdminActionArgs,
} from "../types";

export const MODIFY_VAULT_SETTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([26]);

export function getModifyVaultSettingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 1).encode(MODIFY_VAULT_SETTING_DISCRIMINATOR);
}

export type ModifyVaultSettingInstruction<
  TProgram extends string = typeof EXPONENT_CORE_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountAdminState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountAdminState extends string ? ReadonlyAccount<TAccountAdminState> : TAccountAdminState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ModifyVaultSettingInstructionData = {
  discriminator: ReadonlyUint8Array;
  action: AdminAction;
};

export type ModifyVaultSettingInstructionDataArgs = { action: AdminActionArgs };

export function getModifyVaultSettingInstructionDataEncoder(): Encoder<ModifyVaultSettingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 1)],
      ["action", getAdminActionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: MODIFY_VAULT_SETTING_DISCRIMINATOR })
  );
}

export function getModifyVaultSettingInstructionDataDecoder(): Decoder<ModifyVaultSettingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
    ["action", getAdminActionDecoder()],
  ]);
}

export function getModifyVaultSettingInstructionDataCodec(): Codec<
  ModifyVaultSettingInstructionDataArgs,
  ModifyVaultSettingInstructionData
> {
  return combineCodec(
    getModifyVaultSettingInstructionDataEncoder(),
    getModifyVaultSettingInstructionDataDecoder()
  );
}

export type ModifyVaultSettingInput<
  TAccountVault extends InstructionAccountInput = InstructionAccountInput,
  TAccountSigner extends InstructionSignerInput = InstructionSignerInput,
  TAccountAdminState extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  vault: TAccountVault;
  signer: TAccountSigner;
  adminState: TAccountAdminState;
  systemProgram?: TAccountSystemProgram;
  action: ModifyVaultSettingInstructionDataArgs["action"];
};

export function getModifyVaultSettingInstruction<
  TAccountVault extends InstructionAccountInput,
  TAccountSigner extends InstructionSignerInput,
  TAccountAdminState extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof EXPONENT_CORE_PROGRAM_ADDRESS,
>(
  input: ModifyVaultSettingInput<
    TAccountVault,
    TAccountSigner,
    TAccountAdminState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ModifyVaultSettingInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountVault, InstructionAccountInputAddress<TAccountVault>>,
  ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
  ResolvedInstructionAccountMeta<
    TAccountAdminState,
    InstructionAccountInputAddress<TAccountAdminState>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? EXPONENT_CORE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isSigner: false, isWritable: true },
    signer: { value: input.signer ?? null, isSigner: true, isWritable: true },
    adminState: { value: input.adminState ?? null, isSigner: false, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("adminState", accounts.adminState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getModifyVaultSettingInstructionDataEncoder().encode(
      args as ModifyVaultSettingInstructionDataArgs
    ),
    programAddress,
  } as ModifyVaultSettingInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountVault, InstructionAccountInputAddress<TAccountVault>>,
    ResolvedInstructionAccountMeta<TAccountSigner, InstructionAccountInputAddress<TAccountSigner>>,
    ResolvedInstructionAccountMeta<
      TAccountAdminState,
      InstructionAccountInputAddress<TAccountAdminState>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedModifyVaultSettingInstruction<
  TProgram extends string = typeof EXPONENT_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    signer: TAccountMetas[1];
    adminState: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ModifyVaultSettingInstructionData;
};

export function parseModifyVaultSettingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedModifyVaultSettingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      signer: getNextAccount(),
      adminState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getModifyVaultSettingInstructionDataDecoder().decode(instruction.data),
  };
}
