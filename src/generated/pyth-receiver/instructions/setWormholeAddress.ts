/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS } from "../programs";

export const SET_WORMHOLE_ADDRESS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  154, 174, 252, 157, 91, 215, 179, 156,
]);

export function getSetWormholeAddressDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_WORMHOLE_ADDRESS_DISCRIMINATOR);
}

export type SetWormholeAddressInstruction<
  TProgram extends string = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetWormholeAddressInstructionData = {
  discriminator: ReadonlyUint8Array;
  wormhole: Address;
};

export type SetWormholeAddressInstructionDataArgs = { wormhole: Address };

export function getSetWormholeAddressInstructionDataEncoder(): FixedSizeEncoder<SetWormholeAddressInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["wormhole", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_WORMHOLE_ADDRESS_DISCRIMINATOR })
  );
}

export function getSetWormholeAddressInstructionDataDecoder(): FixedSizeDecoder<SetWormholeAddressInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["wormhole", getAddressDecoder()],
  ]);
}

export function getSetWormholeAddressInstructionDataCodec(): FixedSizeCodec<
  SetWormholeAddressInstructionDataArgs,
  SetWormholeAddressInstructionData
> {
  return combineCodec(
    getSetWormholeAddressInstructionDataEncoder(),
    getSetWormholeAddressInstructionDataDecoder()
  );
}

export type SetWormholeAddressInput<
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
  payer: TAccountPayer;
  config: TAccountConfig;
  wormhole: SetWormholeAddressInstructionDataArgs["wormhole"];
};

export function getSetWormholeAddressInstruction<
  TAccountPayer extends InstructionSignerInput,
  TAccountConfig extends InstructionAccountInput,
  TProgramAddress extends Address = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
>(
  input: SetWormholeAddressInput<TAccountPayer, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): SetWormholeAddressInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
  ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isSigner: true, isWritable: false },
    config: { value: input.config ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [getAccountMeta("payer", accounts.payer), getAccountMeta("config", accounts.config)],
    data: getSetWormholeAddressInstructionDataEncoder().encode(
      args as SetWormholeAddressInstructionDataArgs
    ),
    programAddress,
  } as SetWormholeAddressInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
  >);
}

export type ParsedSetWormholeAddressInstruction<
  TProgram extends string = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetWormholeAddressInstructionData;
};

export function parseSetWormholeAddressInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetWormholeAddressInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payer: getNextAccount(), config: getNextAccount() },
    data: getSetWormholeAddressInstructionDataDecoder().decode(instruction.data),
  };
}
