/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS } from "../programs";
import {
  getDataSourceDecoder,
  getDataSourceEncoder,
  type DataSource,
  type DataSourceArgs,
} from "../types";

export const SET_DATA_SOURCES_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  107, 73, 15, 119, 195, 116, 91, 210,
]);

export function getSetDataSourcesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_DATA_SOURCES_DISCRIMINATOR);
}

export type SetDataSourcesInstruction<
  TProgram extends string = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetDataSourcesInstructionData = {
  discriminator: ReadonlyUint8Array;
  validDataSources: Array<DataSource>;
};

export type SetDataSourcesInstructionDataArgs = { validDataSources: Array<DataSourceArgs> };

export function getSetDataSourcesInstructionDataEncoder(): Encoder<SetDataSourcesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["validDataSources", getArrayEncoder(getDataSourceEncoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_DATA_SOURCES_DISCRIMINATOR })
  );
}

export function getSetDataSourcesInstructionDataDecoder(): Decoder<SetDataSourcesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["validDataSources", getArrayDecoder(getDataSourceDecoder())],
  ]);
}

export function getSetDataSourcesInstructionDataCodec(): Codec<
  SetDataSourcesInstructionDataArgs,
  SetDataSourcesInstructionData
> {
  return combineCodec(
    getSetDataSourcesInstructionDataEncoder(),
    getSetDataSourcesInstructionDataDecoder()
  );
}

export type SetDataSourcesInput<
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
  payer: TAccountPayer;
  config: TAccountConfig;
  validDataSources: SetDataSourcesInstructionDataArgs["validDataSources"];
};

export function getSetDataSourcesInstruction<
  TAccountPayer extends InstructionSignerInput,
  TAccountConfig extends InstructionAccountInput,
  TProgramAddress extends Address = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
>(
  input: SetDataSourcesInput<TAccountPayer, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): SetDataSourcesInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
  ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isSigner: true, isWritable: false },
    config: { value: input.config ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [getAccountMeta("payer", accounts.payer), getAccountMeta("config", accounts.config)],
    data: getSetDataSourcesInstructionDataEncoder().encode(
      args as SetDataSourcesInstructionDataArgs
    ),
    programAddress,
  } as SetDataSourcesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
  >);
}

export type ParsedSetDataSourcesInstruction<
  TProgram extends string = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetDataSourcesInstructionData;
};

export function parseSetDataSourcesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetDataSourcesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payer: getNextAccount(), config: getNextAccount() },
    data: getSetDataSourcesInstructionDataDecoder().decode(instruction.data),
  };
}
