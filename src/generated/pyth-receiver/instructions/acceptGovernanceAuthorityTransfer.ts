/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS } from "../programs";

export const ACCEPT_GOVERNANCE_AUTHORITY_TRANSFER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([254, 39, 222, 79, 64, 217, 205, 127]);

export function getAcceptGovernanceAuthorityTransferDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_GOVERNANCE_AUTHORITY_TRANSFER_DISCRIMINATOR
  );
}

export type AcceptGovernanceAuthorityTransferInstruction<
  TProgram extends string = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptGovernanceAuthorityTransferInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptGovernanceAuthorityTransferInstructionDataArgs = {};

export function getAcceptGovernanceAuthorityTransferInstructionDataEncoder(): FixedSizeEncoder<AcceptGovernanceAuthorityTransferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ACCEPT_GOVERNANCE_AUTHORITY_TRANSFER_DISCRIMINATOR })
  );
}

export function getAcceptGovernanceAuthorityTransferInstructionDataDecoder(): FixedSizeDecoder<AcceptGovernanceAuthorityTransferInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getAcceptGovernanceAuthorityTransferInstructionDataCodec(): FixedSizeCodec<
  AcceptGovernanceAuthorityTransferInstructionDataArgs,
  AcceptGovernanceAuthorityTransferInstructionData
> {
  return combineCodec(
    getAcceptGovernanceAuthorityTransferInstructionDataEncoder(),
    getAcceptGovernanceAuthorityTransferInstructionDataDecoder()
  );
}

export type AcceptGovernanceAuthorityTransferInput<
  TAccountPayer extends InstructionSignerInput = InstructionSignerInput,
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
  payer: TAccountPayer;
  config: TAccountConfig;
};

export function getAcceptGovernanceAuthorityTransferInstruction<
  TAccountPayer extends InstructionSignerInput,
  TAccountConfig extends InstructionAccountInput,
  TProgramAddress extends Address = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
>(
  input: AcceptGovernanceAuthorityTransferInput<TAccountPayer, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): AcceptGovernanceAuthorityTransferInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
  ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
> {
  // Program address.
  const programAddress = config?.programAddress ?? PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isSigner: true, isWritable: false },
    config: { value: input.config ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [getAccountMeta("payer", accounts.payer), getAccountMeta("config", accounts.config)],
    data: getAcceptGovernanceAuthorityTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptGovernanceAuthorityTransferInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>,
    ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
  >);
}

export type ParsedAcceptGovernanceAuthorityTransferInstruction<
  TProgram extends string = typeof PYTH_SOLANA_RECEIVER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: AcceptGovernanceAuthorityTransferInstructionData;
};

export function parseAcceptGovernanceAuthorityTransferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAcceptGovernanceAuthorityTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payer: getNextAccount(), config: getNextAccount() },
    data: getAcceptGovernanceAuthorityTransferInstructionDataDecoder().decode(instruction.data),
  };
}
